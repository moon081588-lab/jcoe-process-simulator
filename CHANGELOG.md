# 변경 이력

세아제강 JCOE 공정 시뮬레이터. 최신 항목이 위입니다.

---

## (최신) 2026-08-19 (4) 오더 간트에서 바로 펼쳐 보기 + 시간 구성 분해

> "페이지가 따로 되어 있으니 보기 조금 불편한 거 같아. 오더 간트 페이지에서 바로 볼 수 있게 숨김/보이기를 추가해.
>  ① 막대를 누르면 밑에 이 오더의 계산이 어떻게 되어 있는지
>  ② 앞선 공정에서 끝나지 않아 발생하는 대기시간, tool change 시간 등이 간트 차트에 포함되어 있을 텐데
>     그 시간까지 구별해서 같이 볼 수 있는 방법"

### ① 간트 행을 누르면 **그 자리에서** 펼쳐집니다

탭을 옮기지 않습니다. 행 바로 아래에 상세 패널이 열리고, 다시 누르면 접힙니다(한 번에 하나).
패널 안에서 **✎ 편집도 그대로** 됩니다 — 값을 고치면 막대·분해·산식이 함께 다시 계산됩니다.
크게 보고 싶으면 「산식 검증 탭에서 크게 보기 ▸」 로 넘어갑니다.

### ② 막대가 무엇으로 채워져 있는지 — 네 가지로 나눴습니다

| | 뜻 |
|---|---|
| **가공** | 설비가 이 오더의 파이프를 실제로 깎고 있던 시간 |
| **전환** | 공구·다이 교체(tool change) — 앞 제품과 규격이 달라 생긴 시간 |
| **대기** | 공장은 도는데 이 오더가 **아무 설비도 못 잡은** 시간 (앞 공정이 안 끝났거나, 설비가 다른 오더에 물렸거나) |
| **비가동** | 교대 종료·점심·주말 |

**네 값의 합은 막대 길이와 정확히 같습니다** (전 오더에서 검증 — `verify_gantt.js` ②).

두 가지 관점을 나란히 놓았습니다.

- **오더 전체** — 예: `총 193.1h = 가공 121.1h(63%) + 비가동 72.0h(37%)`
  «가공» 은 이 오더의 파이프 중 **하나라도** 설비에 물려 있던 시간입니다(404본이 겹쳐 흐릅니다).
- **본 1개 리드타임** — 예: `총 73.8h = 가공 2.6h(4%) + 전환 1.3h(2%) + 대기 42.9h(58%) + 비가동 27.0h(37%)`
  **표준시간은 2.6h 인데 실제로는 73.8h 걸리는 이유**가 여기서 한 줄로 보입니다.

그 아래 **공정별 표**가 대기 · 전환 · 가공 · 실행 구간을 공정마다 적습니다
(예: `12M E/M 대기 26.2h · 전환 — · 가공 10분 · 03-03 20:12 → 20:22`).
본 번호와 확관 호기를 바꿔 가며 볼 수 있습니다.

### 막대 안에도 칠했습니다

간트 막대 안을 **가공(초록) · 전환(주황)** 으로 칠하고, 나머지 옅은 바탕이 대기·비가동입니다.
어느 오더가 «일하느라» 긴지, «기다리느라» 긴지 목록에서 바로 구분됩니다.
「막대 안에 시간 구성 표시」 체크로 끄고 켤 수 있습니다.

### 구현 메모

- `flow.js` 에 `orderTimeSplit()` · `pipeTimeSplit()` 신설. 구간 병합(`mergeSpans`)·차집합(`subtractSpans`)으로
  겹치지 않게 나눈 뒤 **가동시간 기준**으로 재므로 합이 정확히 떨어집니다. R/B 라인 오더는 R/B 근무조로 잽니다.
- 산식 카드 렌더러가 화면 두 곳을 쓰게 되면서, 상태를 DOM 입력칸이 아니라 **`VFS` 객체**로 옮겼습니다.
  종전에는 `vfCompute` 가 `$('vfOrder')` 를 직접 읽어 다른 화면에서는 쓸 수 없었습니다.
- 값을 고쳐 간트가 다시 그려질 때 **열어 둔 편집 패널을 그대로 복원**합니다(공정 구성이 같을 때만).
  이게 없으면 간트 안에서는 한 번 고칠 때마다 편집칸이 닫혀 연속 편집이 불가능합니다.

### 새 시험 — `tools/verify_gantt.js` (시험 파일 10개)

```
① 펼치기·닫기 · 한 번에 하나 · 다른 행으로 이동
② 막대 분해의 합 = 막대 길이            (58오더 전부, 불일치 0)
③ 본 리드타임 분해의 합 = 리드타임      (58오더 전부, 불일치 0)
④ 공정별 표 = 그 본의 실제 이벤트        (16공정 일치)
⑤ 패널 안 산식 카드 · ✎ 편집 · 되돌리기
⑥ 본 번호 변경이 표·카드에 전파
⑦ 막대 시간 구성 표시 토글 · ✕ 닫기
⑧ 콘솔 오류 0
```

10종 전부 PASS 입니다.

---

## (최신) 2026-08-19 (3) 3차 전수 감사 — 결함 17건 수정

지금까지 만든 것이 의도대로 도는지 **다섯 영역(엔진 · 시뮬레이션 · 산식 검증 UI · 기준정보 입출력 ·
전 화면 런타임)을 나눠 전수 조사**했습니다. 기존 시험 7종은 전부 통과하는 상태였고,
아래 결함은 **모두 그 사각지대**에 있었습니다. 전부 재현한 뒤 고쳤습니다.

### ★ 가장 심각 — 「산식 검증」이 공정 이름과 값을 어긋나게 짝지었다

편집 패널을 열어 둔 채 설정을 바꿔 **공정 수가 달라지면**, 카드는 그대로 두고 숫자만 갈아 끼우던
제자리 갱신(`vfPatch`)이 값을 **한 칸씩 밀어** 넣었습니다.

| 카드 제목 | 표시된 값 | 실제 |
|---|---|---|
| 면취 공정 | **300 s** (Calibration Press 값) | 585 s |
| 수압 Test | 572 s (면취 값) | 745 s |
| JCOE 포장 | 1,308 s (F-X ray 값) | 853 s |

마지막 공정은 화면에서 아예 사라졌고, 요약은 "17개 통과 공정"인데 카드는 16개였습니다.
**콘솔 오류 없이 조용히 틀렸습니다.**

→ 카드마다 공정 ID(`data-nid`)를 심고, 제자리 갱신 전에 **공정 구성이 그대로인지 대조**합니다
(`vfSameShape`). 달라졌으면 편집 패널을 잃더라도 다시 그립니다 — 틀린 값을 보여주는 것보다 낫습니다.

### ★ 산식 검증이 시뮬레이션과 다른 값을 보여주고 있었다

「이 오더가 이렇게 계산됐다」를 **오더 단위**로 재현했는데, 시뮬레이션은 **본(pipe) 단위**입니다.

| 항목 | 종전 | 결과 |
|---|---|---|
| 확관 호기 | 오더의 **첫 본** 호기를 오더 전체에 적용 | 1,446본 중 **505본** 불일치. 최악: 화면 345s / 실제 1,176s (**3.4배**) |
| 전역 누적 본 번호 | 오더 내 번호로 **추측** | 1,446본 중 **242본**에서 포장 시간 불일치 (−250s, 22%) |
| 대사표 | 1본 산식 vs **오더 전 본의 평균** | 변동성·보정을 꺼도 58오더 중 **42오더**가 「어긋남」 색 |

기본 오더셋 58개 중 **29개가 #1·#2호기에 걸쳐** 배정됩니다. 오더 단위로는 맞을 수가 없었습니다.

→ 시뮬레이션 이벤트에 **전역 누적 본 번호(`g`)** 를 실었고, 화면은 `k`번째 본의 **실제 호기·실제
전역 번호**를 이벤트에서 꺼내 씁니다. 대사표도 **같은 본과 1:1** 로 맞췄습니다.
이제 **21,388건(58오더 × 1,446본 × 전 공정) 전부 일치**합니다 (`verify_audit2.js` D1).

### ★ 전환시간이 «합산» 되고 있었다 — 화면 설명과 정반대

2026-08-06 세아제강 확정 사항은 **"셋업은 상위값만 적용. 헤드+다이 동시 교체 시 150분(합산 아님)"**
이고, 화면도 "셋 중 **가장 큰 값 하나만** 적용됩니다" 라고 안내합니다. 확관의 공구 계층 룰은
처음부터 상위값만 썼는데, **일반 공정 경로만 `od + t + L` 을 더하고** 있었습니다.

- 총 전환시간 **265.9h → 240.1h** (−25.8h, **−9.7%**), 완료 24.36일 → 24.30일

### ★ 값 오염 방어가 통째로 죽어 있었다

`refSetTbl`/`refSet` 의 되돌리기 판정은 `!isFinite(SIM.kpi.makespanH)` 였는데,
`flow.js` 가 비유한 종료시각을 **걸러내고** makespan 을 내므로 이 값은 **언제나 유한**합니다.
즉 **한 번도 발동하지 않는 죽은 코드**였습니다. 실제로는:

- JSON 의 `엑셀표재정의` 에 `0` 하나 → 완료일 **192만 일**, 걸러낸 항목 0개, 경고 없음
- 음수 → 소요시간·가동률이 **음수**가 되는데 경고 없음
- 화면에서 용접속도를 `1e-9` 로 → 완료일 **51만 일**, 가동률 **2억%**, 경고 없음

고친 것:

| | 종전 | 지금 |
|---|---|---|
| `setRefTbl` | `isFinite` 만 확인 | **양수만** 수용 (표 값은 속도·거리·시간·횟수) |
| 불러오기 검증 | `+v` 강제변환 → `null`·`false`·`""` 가 0 으로 통과 | **타입부터** 확인 · 0 이하 거부 · 원값의 1/1000~1000배 범위 |
| 편집칸 하한 | 무조건 `1e-9` | **그 칸 기본값의 1/1000 ~ 1000배** |
| 되돌리기 판정 | `!isFinite(makespanH)` (죽은 코드) | `refInsane()` — 공정 가동시간·가동률의 음수/무한대, 기준선 대비 100배 폭주 |

### ★ 「고쳤는데 안 바뀐다」를 만드는 먹통 재정의 574개

`refTblCellExists` 가 **엔진이 실제로 조회하지 않는 칸**을 유효로 판정했습니다 —
통과한 821개 키 중 **574개(70%)** 가 저장만 되고 계산에는 아무 영향이 없었습니다
(`dieSpec|M1`, `tackWeld|length`, `tackWeld|00|2`, `constructor|name` …).
게다가 그런 키가 하나만 남아도 `refCount()>0` 이 되어 **기준선이 얼어붙고**, 그 뒤 계획서를 바꾸면
그 차이가 「기준정보 편집 덕분」인 것처럼 ±일로 표시됐습니다 — 2026-08-14 에 고쳤다고 적은
바로 그 증상이 새 경로로 되살아난 것입니다.

→ 판정을 엔진(`refTblKeyValid`)으로 옮겨 **`tblVal` 이 만드는 키 형태와 정확히 같은 것만** 통과시킵니다
(정규 행 표기 + 조회에 쓰이는 열만). 엔진이 만드는 **243개 키는 전부 통과, 위조·먹통 키 22종은 전부 거부**.
기준선도 오더셋·교대·규칙 **지문**을 함께 저장해, 조건이 달라지면 비교를 포기합니다.

### 그 밖에 고친 것

| # | 결함 | 영향 |
|---|---|---|
| 1 | Gap Press 「적용 산식」에 `${P.x70}` **소스 코드가 그대로 노출** | X65·t31.8 같은 제품에서 화면에 코드 조각이 보임 |
| 2 | `nf()` 가 0.0005 미만을 `0` 으로 찍어 「123**/0**」 | 검증 화면의 근거 문자열이 실제와 달라짐 |
| 3 | Pre Bender 성형 횟수가 **음수** 가능 (`lead` > 관 길이) | 음수 표준시간·음수 가동률이 KPI 에 그대로 |
| 4 | R/B 확관 표기식이 `max(N−2,0)` 클램프 미반영 | 짧은 시편에서 화면과 엔진이 15~150초 어긋남 |
| 5 | 17m 이상 제품은 수압 충수시간을 **편집 불가**, 이유도 "계획서 값"이라 잘못 안내 | 18M 라인 제품 현장값 반영 불가 |
| 6 | 「산식 검증」 편집이 **기준정보 탭 입력칸에 미반영**, 거기서 ↺ 를 누르면 편집이 조용히 삭제 | 자기 편집을 모르고 지움 |
| 7 | 같은 공정이 두 번 나오는 오더(관단 R/T·F-X ray)에서 두 편집칸이 따로 놈 | 화면값 ≠ 적용값 |
| 8 | `REF_POSITIVE_KEYS` 정규식이 **분모가 아닌 7개 키**에 하한을 걸어 정상값 0 을 막음 | `Packing.extraEvery = 0`(추가검사 안 함)을 넣을 수 없었음 → 화이트리스트로 교체 |
| 9 | 상단 선택기에 범위 밖 값(999·0·2.7)이 **입력칸에 그대로 남고** 계산만 보정됨 | 화면과 계산이 어긋나 보임 |
| 10 | **2D ↺** 가 내부 시각만 되돌리고 시계·완료본수·가동률·타임라인은 옛 시점 그대로 | 리셋 직후 화면 전체가 거짓 |
| 11 | **2D 타임라인이 재생 중 전혀 안 움직임** — `syncSeek()` 이 정의만 되고 **호출하는 곳이 0곳** | 지금 어느 시점인지 알 수 없음 |
| 12 | **3D ↺** 뒤 「포장 완료」 카운터에 직전 재생 값이 잔존 | 시작 화면에 실적처럼 보임 |
| 13 | **표준시간 계산기에 관단 R/T 누락** | 같은 제품인데 산식 검증과 1본 총시간이 400s 달랐음 (관단탭 절단은 「측정 대상 외」로 명시) |
| 14 | 「전후 비교」가 최적화를 먼저 돌리면 **영구히 빈칸** | 기준이 없으면 EAT 로 한 번 돌려 만들도록 |
| 15 | 기준정보 「설비 대수」 설명에 **없어진 #3호기 토글** 안내 잔존 | 2026-08-06 제거된 조작을 안내 |

### 새 시험 2종 — 시험 파일 9개

```
tools/verify_audit2.js    (node)        A 산식 표기 · B 계산 정확성 · C 표 재정의 방어 · D 시뮬 일치
                                        → D1 「21,388건 전부 일치」가 핵심 회귀 방지선
tools/verify_vfedit2.js   (playwright)  ① 공정 구성 변화 시 카드 밀림 ② 폭주 방어 ③ 두 탭 동기화
                                        ④ 같은 상수 두 카드 ⑤ 본 번호별 호기·전역순번 ⑥ 입력 보정
                                        ⑦ 2D·3D 리셋과 타임라인
```

9종 전부 PASS 입니다.

### 결함이 아니라고 확인한 것 (근거 포함)

- **`pickRange`/`pickInch` 리팩터링 회귀 없음** — 옛 구현과 경계·구멍·범위 밖·무작위 포함 **60,758건 대조, 차이 0**
- **표 재정의 키 충돌 없음** — 무작위 3,000 사양에서 나온 참조 40,492건 / 서로 다른 키 242개, 중복 대응 0
- **프로토타입 오염 없음** · **XSS 없음**(오더번호에 `<img onerror>` 주입 시 미실행)
- **`verifyOrder` 의 경로·인자는 `simulate` 와 동일** — 11개 설정 조합 × 21,388 스텝 오차 0
- **`planload` 5종 샘플 정상** (40/1767 · 40/1847 · 21/784 · 15/469 · 58/1446 전부 기대값 일치)
- **`prodlog` 의 `perPipeSec`↔`paceSec` 뒤바뀌지 않음**
- **2D·3D 화면 결과 일치** — seek 두 지점에서 20개 노드 전부 동일
- **콘솔 오류·미처리 예외 0건** — 전 시나리오 통틀어 (위 결함들은 **전부 조용한 오동작**이었습니다)

---

## (최신) 2026-08-19 (2) 「산식 검증」 화면에서 [편집] — 그 자리에서 값을 고친다

> "이전에 파라미터 값을 바꾸는 걸 엑셀 표로 했던 것 같은데, 이런 화면에서도 [편집] 버튼을 누르면
>  곧바로 편집이 가능해지도록 하는 건 어때"

공정 카드마다 **`✎ 편집`** 버튼이 붙었습니다. 누르면 그 공정에 실제로 쓰인 값이 입력칸으로 바뀌고,
고치는 즉시 **그 카드의 산식·계산·소요시간과 시뮬레이션 전체가 다시 계산**됩니다.

### 두 종류를 한곳에 모았습니다

| 구분 | 예 | 종전 |
|---|---|---|
| **기준정보 상수** — 산식에 박힌 값 | 기본 46.5s · 피치 분모 290 · 1스트로크 가산 17.2s | 「기준정보」 탭에서만 |
| **엑셀 표에서 조회된 값** — 이 제품이 짚은 **그 칸 하나** | 성형 피치 1,450mm (두께 8~25.4 구간) | **어디서도 못 고쳤음** |

두 번째가 이번의 핵심입니다. `emSpeed`·`preBenderPitch`·`insideWeld` 같은 룩업 표는
「기준정보」 탭에서 **행 수만** 보여 주고 값은 보기 전용이었습니다. 고치려면 엑셀을 수정해
`tables.json` 을 다시 뽑고 `build.py` 를 돌려야 했습니다.

이제 엔진에 **표 값 재정의 계층**(`REF.tbl`)을 뒀습니다. 원표(`T`)는 건드리지 않고,
조회가 짚은 셀 하나만 덮어씁니다.

```
키 형식   범위표  <표이름>|<행번호>|<열번호>     예: preBenderPitch|0|2
          인치표  <표이름>|<인치>                예: pressX1|30
          상수    <표이름>|<키>                  예: hydroConst|airVent_36up
```

- 표 전체(수백 행)를 펼치지 않습니다. **이 제품이 실제로 짚은 칸만** 보여 주므로
  "두께 9.3mm 짜리 내면 SAW 속도가 실제와 다르다" 를 그 줄에서 바로 고칠 수 있습니다.
- 고친 칸은 파라미터 표에 `*` 와 노란색으로 표시되고, 「기준정보」 탭 룩업 표 목록에도
  「n칸 수정」으로 뜹니다.
- **내보내기 JSON 에 `엑셀표재정의` 항목으로 함께 저장**됩니다. 불러올 때는 키가 실제 칸을
  가리키는지 검사해서, 없는 표·없는 행을 조용히 무시하지 않고 걸러낸 목록으로 알려 줍니다.

### 고칠 수 없는 값은 이유를 밝힙니다

길이·두께·외경처럼 **계획서에서 읽은 값**과, 성형 횟수 n 처럼 **위 값들로부터 계산되는 값**은
편집 대상이 아닙니다. 패널 하단에 그 목록과 이유를 적어 두었습니다.

### 카드를 다시 그리지 않습니다

값을 고치면 `runSim()` 이 다시 도는데, 여기서 카드를 통째로 `innerHTML` 로 다시 만들면
**방금 누른 입력칸이 사라져 두 번째 입력부터 조용히 버려집니다** — 2026-08-14 전수 감사에서
「기준정보」 탭이 겪었던 바로 그 결함입니다. 그래서 편집 패널이 열려 있는 동안에는
`[data-f]` 로 표시된 숫자 칸만 제자리에서 갈아 끼웁니다.

### 새 시험 — `tools/verify_vfedit.js`

이 결함이 되살아나지 않도록 브라우저에서 직접 확인합니다.

```
① 편집 칸이 뜬다 (기준정보 상수 + 엑셀 표 칸)
② 고치면 소요시간·산식 계산·파라미터 표가 바뀐다
③ 편집 패널이 열린 채 유지되고 **두 번째 입력도 반영된다**   ← 회귀 방지
④ 「전부 원래대로」 로 소요시간·완료일이 원래대로 돌아온다
⑤ 콘솔 오류 없음
전 항목 PASS
```

`verify_calc.js` 에도 표 재정의 회귀 6항목(값 반영 · 표기식 일치 · 되돌리기 · 프로토타입 오염 ·
문자열 거부)을 넣었습니다. 시험 파일은 **7개**가 됐습니다.

---

## 2026-08-19 (1) 「산식 검증」 탭 신설 — 어떤 식에 어떤 값이 들어가 이 숫자가 나왔는가

> "어떤 식에 어떠한 파라미터 값이 들어가서 이러한 값이 나왔다라는 걸 다 검증하고 싶어.
>  오더 간트를 누르면 이러한 정보들을 볼 수 있는 tab 이 있으면 좋겠어"

시뮬레이터가 내놓는 모든 공정시간을 **식 → 값 → 계산** 세 단계로 펼쳐 봅니다.

### 화면 — 오더 간트 → 행 클릭 → 「산식 검증」

오더 간트의 각 행에 「산식 검증 ▸」 버튼이 붙었습니다. 누르면 그 오더가 지나가는 **모든 공정**이
차례대로 카드로 펼쳐지고, 카드마다 세 블록이 나옵니다.

| 블록 | 내용 | 예 (Edge Miller) |
|---|---|---|
| **적용 산식** | 기호 그대로의 식 | `348s − (123/고속값) + (전진거리/215.6) + 길이×(0.06/고속값 − 0.0016) + 110s(첫 본)   [18M 일반]` |
| **파라미터 값 (이 제품)** | 값 + **출처** | 길이 18,288mm (계획서) / 밀링 고속값 4 m/min (엑셀 emSpeed 표, 두께 18.001~100) / 판재 이송거리 19,250mm (엑셀 emFeed 표) … |
| **산식 계산** | 숫자를 대입한 식 = 결과 | `348 − 123/4 + 19,250/215.6 + 18,288×(0.06/4−0.0016) + 110 = 762 s` |

- 파라미터마다 **어디서 온 값인지**를 함께 적습니다 — 계획서 / 엑셀 표(어느 구간인지까지) /
  기준정보 / 다이표 / 시뮬레이터 판정. 현직자가 "이 4라는 숫자는 어디서 나왔나"를 되묻지 않아도 됩니다.
- 상단 선택기로 **오더 · 오더 내 본 번호 · 전역 누적 본 번호 · 확관 호기**를 바꿔 가며 볼 수 있습니다.
  (첫 본 가산 110s, 포장 10본마다 추가검사 250s 처럼 **본 번호에 따라 달라지는 항목**을 직접 확인)
- 맨 아래 **「시뮬레이션 실행값과 대사」** 표 — 이 오더가 실제 시뮬레이션에서 각 공정에 머문
  평균 시간과 위 산식 결과를 나란히 놓고 차이(%)를 보여 줍니다. 변동성·실적 보정을 끄면 정확히 0% 입니다.

### 엔진 — 20개 표준시간 함수 전부에 검증 메타 부착

`STD.*` 의 모든 반환값에 `tpl`(적용 산식) · `vars`(파라미터 표) · `subst`(대입 계산)을 붙였습니다
(`withCalc()` 헬퍼). **계산 결과 자체는 한 글자도 바뀌지 않았습니다** — `verify_formulas.js` 전 항목 PASS 로 확인.

### 새 시험 — `tools/verify_calc.js`

화면에 찍히는 「산식 계산」 문자열을 **실제로 계산해서** 엔진 값과 대조합니다.
표기와 엔진이 어긋나면 곧바로 FAIL 이 납니다.

```
대표 사양 4종 × 라인 2종 × 본 번호 2종 × 전 공정 (확관은 4호기 모두)
검사 288건 / 불일치 0건 → 전 항목 PASS
```

이로써 시험 파일은 **6개**가 됐습니다 (formulas · audit · prodlog · static · calc · ui).

---

## 2026-08-14 세아제강 회신 반영 — 확관 산출식 이미지 추출 · R/B 스펙 · 실적 로그 검증

세아제강 회신 5건과 자료 2종(「★JCOE 공정 생산 표준 시간 분석 20251231 (POSTECH 송부).xlsx」,
`machine_prod_log` 스냅샷 실적 CSV)을 반영했습니다.

### ① 「Expander(2호기) 시트가 비어 있다」의 진짜 원인 — 이미지

> "expander(2호기)에 대한 시트가 비어있는 이유는 …정보가 이미지 형식으로 되어있기 때문입니다.
>  이런 경우 파일 한번 열어서 원인 파악 해주시면 좋을 거 같습니다!"

지적이 정확했습니다. xlsx 는 ZIP 이고 그림은 셀이 아니라 `xl/media/` 에, 위치는 `xl/drawings/` 에
따로 들어가므로 **셀만 훑는 추출기로는 원리적으로 보이지 않습니다.**

- **`tools/extract_images.py` 신설** — 시트별 [값 있는 셀 / 이미지 / 메모] 를 세고,
  이미지를 `<시트명>__<앵커셀>__<파일명>.png` 로 꺼냅니다. 앞으로 시트를 "비어 있다"고
  판정하기 전에 반드시 먼저 돌립니다.
- 20251231 송부본에서 **확관 산출식 3장**을 찾았습니다 (`source/data/evidence/` 에 보관).

| 앵커 | 찾은 내용 |
|---|---|
| `Total Summary!S22` (1호기) | 확관 Step Size = **다이 Size − 150mm** · `N = ROUNDUP(L / StepSize)` · 예시 `12,802/(550−150) = 33회` |
| `Total Summary!S23` (2호기) | `N = ROUNDUP((L−(S_start+S_end))/(F−O)) + 2 + α` · 메모 「N 은 항상 **홀수**」 |
| `Expander(RB)!J4` (R/B) | 확관 Step Size = **다이 Size − 90mm** |

- **엑셀 #1호기 N 식을 `ceil(L/(step−150))` 로 정정**했습니다(종전 `ceil((L−500)/step)+2`).
  이로써 엑셀식과 정본 `specs.py` 는 **분모가 같고 올림/사사오입만 다른 사실상 같은 식**임이 확인됩니다.
- **#2호기는 엑셀(홀수)과 정본(짝수)이 정반대**입니다. 2026-08-06 회신대로 정본을 기본값으로 두고 재확인 요청.
- 20251231 판에는 1·2호기 시트 자체가 없어 `die_specs_m2` 68행 대조는 **원본 재요청 중**입니다.

### ② OD1219 `44t(250mm)` / `44t(700mm)` 중복 — F/L 이 다른 다이 2개

같은 엑셀 `Expander(RB)` 시트의 세아제강 셀 메모가 괄호 숫자를 **F/L(Full Length)** 로 씁니다
(「32"x34 die (F/L 700mm)」, 「다이 40" 44t 사용할 것 · FL 300mm」). 따라서 중복 표기가 아니라
**두께 44t 짜리 F/L 250mm 다이와 F/L 700mm 다이**, 서로 다른 실물 공구입니다.
헤드는 인치로 정상 매핑됩니다(OD1219 = 48" → #1호기 헤드 #1000, R/B 헤드 #800).

- `toolInfo` 가 동점 후보를 `alts` 로 남기고, `dieDuplicates(machine)` 로 전체 중복 칸을 나열합니다.
- 어느 다이를 거는지에 따라 확관 소요가 **1,767s vs 507s (3.5배)** 로 갈립니다 — 확인 요청 중.

### ③ R/B 라인 = 확관 다른 호기 — 스펙 전면 반영

| 항목 | 종전 | 반영 |
|---|---|---|
| **확관 Step Size** | 다이 Size 그대로 | **다이 Size − 90mm** |
| 확관 횟수 N | `ceil(L / 다이)` | `ceil(L / (다이−90))` — OD914 t9.3 12.802m 기준 19 → **21회** |
| 소요 | `234 + (N−2)×15` | 동일 (489s → **519s**) |
| **후처리 경로** | R/B 전용만 (수압·2차 U.T 건너뜀) | `FC112 → **HY106 수압 → UT110 2차 U.T** → RT104 → PK112` |
| 가동 조건 | 다이표 외경 & 9≤t≤25.4 & L≤12.8m | 좌동 + **46" 제외** 명시 (`RB_EXCLUDED_INCH`) |
| 열처리 판정 | 병목공정 HT102 열만 | 좌동 + 자재기호 **`C2`** (엑셀 No.20 비고) |

「확관 최적화」 탭에 **R/B 후처리** 토글(`shared` / `dedicated`)을 추가해 구 동작으로 되돌릴 수 있습니다.

### ④ 납기 지연 분석 제거 — 계획서 순서대로만 흘려보내기

> "영업계획납기일로 납기 지연분석을 하지 않고 …조관계획서상에 있는 제품 내역을 순서대로만
>  흘려 보냈을때 어떻게 되는지만 나오면 될 거 같아요."

- **오더 투입일 기본값 = 「계획서 시트 순서대로」**(`dateMode: 'sheet'`) 신설.
  JCOE 시트 행 순서 그대로 시작일부터 전량 투입하고 날짜·납기를 쓰지 않습니다.
- **납기 지연 분석 기본 해제**(`dueAnalysis: false`). 관련 KPI·간트 표시가 사라집니다.

### ⑤ 실적 로그 기반 검증 — 「실적 검증」 탭 · `src/prodlog.js` 신설

- CSV 업로드 → **설비별 실적 본수 · 실적 본당 소요(완료 간격 중위수) · 표준시간 대조 · 실적 오더셋**
- 자재내역에서 외경·두께·길이를 파싱하고, `C2` 강관기호로 옥외 열처리를 판정합니다.
- `tools/verify_prodlog.js` (CLI · 회귀 테스트 12건)

**데이터 해석에서 찾은 것**

| | |
|---|---|
| `PROD_QTY_cum` | **(설비·WO·작업일·근무조) 단위로 리셋** — 전체 누적으로 읽으면 안 됨 |
| `qty_delta` | 스냅샷 유실 구간에서 `delta = cum` 으로 찍힘 → **사용 금지**. PK113 Σdelta 660본 vs 실제 363본 |
| 착수 시각 | 없음 — `LAST_TIME`(완료 시각)만 있어 **완료 간격**으로 본당 소요를 산출 |

**검증 결과 — 포장 표준시간이 실적보다 65% 느립니다 (실적 530s vs 표준식 873s, 비율 0.61).**
수압도 0.79 로 표준식이 느립니다. 나머지 설비는 1.06~2.16 으로 정상 범위입니다.
「포장이 1위 병목(가동률 97%)」이라는 기존 결론이 **과대평가일 가능성**이 있어 산출식 재확인을 요청했습니다.

**실적 본당 추정치 정정 (같은 날 오후)** — 오전에 「포장 실적 530s vs 표준 873s (0.61) →
산출식이 느림」이라고 보고했으나 **측정 오류**였습니다. 완료 간격 분포는 오른쪽 꼬리가 극단적으로
길어 중위수가 가동 중 페이스만 집어내고 대기를 버립니다. 처리량 기준(ΣΔt ÷ ΣΔ누적)으로 다시 재면:

| 포장 PK113 | 중위수 461s | 평균 918s | **처리량 기준 894s** |
|---|---|---|---|

전 설비에서 **실적 ≥ 표준** 이 되고, 포장이 비율 1.20 · **여유율 17% 로 전 설비 중 최저** 입니다.
→ 「포장이 병목」 결론은 뒤집히는 게 아니라 실적으로 **확인**됩니다.
「실적 검증」 탭 기본 지표를 처리량 기준으로 교체하고 **여유율** 열을 추가했습니다.
실적 보정 스위치는 남아 있으나 보정 대상이 없습니다.

**실적 재현** — 실적 오더셋(20건 · 387본)을 3근 · 계획서 순서로 돌리면 makespan **158.5h**,
실제 로그 구간 **154.4h** 대비 **+2.7%** 입니다.

**판단으로 닫은 3건** — ① OD1219 44t 는 **250mm 확정**(700mm 는 대체 다이 — 34t↑ step>400 인
5행이 RB 시트 「대체 작업」 메모와 정확히 겹치고, 44t 나머지 13행은 전부 110~300mm),
② R/B 「열처리 15초 제외」는 **기본 상수 234→219** 로 해석(두 문장이 모두 성립하는 유일한 읽기),
③ #2호기 홀수/짝수는 08-06 회신대로 **specs.py(짝수) 유지**.

### 화면 이동·확대 (2026-08-14)

「공정 흐름」의 시점이 **완전히 고정**돼 있었습니다. `draw()` 가 매 프레임 `fit()` 을 불러
`scale`·`offX`·`offY` 를 다시 계산했기 때문에, 끌어도 그 프레임에 바로 원위치로 돌아갔습니다.

- 기본 배치(`baseScale`/`baseX`/`baseY`)와 **사용자 조작(`VIEW`)** 을 분리해 둘 다 살렸습니다
- **끌기 = 이동 · 휠 = 확대/축소(커서 위치 고정) · 더블클릭 = 원위치**
- 우상단에 배율(%)과 「원위치」 버튼, 좌하단에 조작 안내

「3D 공장」은 조작이 있었지만 이동이 **우클릭 또는 Shift 드래그뿐**이라 고정된 것처럼 느껴졌습니다.

- **가운데 버튼 드래그**로도 이동
- **Ctrl 드래그 = 높이(수직) 이동** — 종전에는 바닥 평면(tx/tz)만 움직여 위아래로 못 갔습니다
- **방향키 / WASD = 이동**, Ctrl+방향키 = 높이, Shift = 큰 폭
- **Shift+휠 = 높이**, `+`/`−` = 확대/축소, `0`/`Home` = 원위치
- 하단 안내 문구에 전부 표기

`verify_ui.js` 가 「끌면 실제로 움직이는지 · 휠로 확대되는지 · 원위치가 되는지」를 매번 확인합니다.

계획 실행 탭 상단의 인용 두 줄(회의 발언)은 지웠습니다.

### 화면 정리 — 정보가 한꺼번에 쏟아지던 문제 (2026-08-14)

기능을 붙일 때마다 설명과 탭이 늘어나 **한 화면에 전부 펼쳐져** 있었습니다.
정보를 줄인 게 아니라 **처음 한 번만 필요한 것을 접었습니다.**

| | 종전 | 지금 |
|---|---|---|
| 설명 박스 | 15개가 **항상 펼쳐짐** — 병목 분석 탭은 상단 1/3 이 설명 | 한 줄 요약 + 「자세히」 접기 |
| 탭 | 12개가 한 줄 | 기본 5개 (계획 실행 · 공정 흐름 · 3D · 기준정보 · 설정) + **「분석 ▾」** 7개 |
| 핵심 수치 | 우상단 11px 회색 글씨 | 헤더 아래 **결과 요약바** 고정 — 완료일 · 1위 병목 · 전환손실 · 생산량 |
| 병목 표 | 9열 | 5열 + 「자세히」 토글 (처리본수·가공h·전환h·전환비중) |
| 설정 탭 | 「SHIFT/일」과 「수압 유지」 **사이에 8줄짜리 교대 시간표 설명**이 끼어 컨트롤 줄이 끊김 | 컨트롤 2줄로 묶고 설명은 아래로 분리·접기 |

효과 — 병목 분석 탭에서 표가 **5행 더** 보이고, 설정 탭 전체가 스크롤 없이 한 화면에 들어옵니다.

- 결과 요약바가 생기면서 중복된 KPI 카드 2개(총 전환시간·전체 계획 소요)와
  헤더 우상단 문자열을 정리했습니다
- 「분석 ▾」 안의 탭을 고르면 버튼에 그 이름이 표시돼 지금 어디에 있는지 보입니다
- `verify_static.js` 에 **화면 밀도 검사** 추가 — 탭 6개 이하 · 설명 박스 기본 접힘 ·
  요약바가 채워져 있는지를 매번 확인합니다 (기능을 더하다 다시 쏟아지는 것을 막습니다)
- 탭이 드롭다운으로 들어가면서 `verify_ui.js` · `verify_static.js` 의 탭 클릭을 `goTab()` 으로 교체

### 2차 전수 감사 — 결함 25건 수정 (2026-08-14)

정적 검사 도구(`tools/verify_static.js`)를 새로 만들고, ui / 3D / 엔진 / 데이터 로더 네 영역을
병렬로 감사했습니다. **전부 재현한 뒤 고쳤고, 회귀 검사에 고정**했습니다.

#### 화면에서 값이 사라지거나 틀리던 것

| 증상 | 원인 | 정정 |
|---|---|---|
| **기준정보에서 값을 연달아 고치면 두 번째부터 조용히 사라짐** | 값 하나 고칠 때마다 `innerHTML` 로 표를 통째로 다시 그려, 옮겨 가려던 칸이 그 순간 사라짐 | 표를 다시 그리지 않고 KPI·미리보기만 제자리 갱신 |
| **3D 가동률이 2D 와 다름** (확관 100% vs 64%) | 3D 만 옛 공식 — 호기별 최댓값이 아니라 평균, 전환시간 누락, R/B 캘린더 미적용 | 2D 와 동일 공식으로 통일 |
| **3D 탭을 한 번 열면 숨겨져 있어도 CPU 코어 하나를 계속 먹음** | 렌더 루프가 표시 여부와 무관하게 매 프레임 실행 (6초 중 6.1초) | 숨겨져 있으면 그리지 않음 |
| **설비 대수를 늘려도 3D·투입산출 탭은 옛 대수** | `setRefCap` 이 자원 풀만 덮고 `NODE[].cap` 은 그대로 | `NODE[].cap` 에 직접 반영 + 대수가 바뀌면 3D 장면 재구성 |
| 3D 재시뮬 후 출하 카운트·설비 정보 패널이 이전 실행 값 유지 | `applySim` 이 화면을 정리하지 않음 | 초기화. 탭 재진입(같은 결과)일 때는 재생 위치 유지 |
| 3D 설비 정보의 관단 R/T 산출식이 실제의 2.5배 | 샘플 spec 의 `rtType` 을 450kV 로 고정 | 노드별 `rtType` 사용 |
| 「원래 대비 ±일」의 **부호가 반대** | 기준선을 부팅 때 한 번만 잡아, 교대·계획서를 바꾼 뒤에도 옛 값과 비교 | 변경 0개일 때마다 자동 재기준 |
| 실적 로그 해제·읽기 실패 후에도 이전 파일의 표·보정 요약이 남음 | 조기 반환 경로에서 패널을 비우지 않음 | 어떤 경로로 나가든 비움 |

#### 계산이 틀리던 것

| 증상 | 원인 | 정정 |
|---|---|---|
| **엑셀 N 모드에서 소형 다이 제품이 폭주** (OD508 t20.25 18.3m → 219,706초, 기본 오더셋 24.4일 → 301일) | 「끝단 150mm 이하면 −100」 규칙이 한 경로에만 있어 `recipe = max(step−150,1)` 이 **1mm** 가 됨 | 다이 크기에 따라 여유값을 정하도록 통일 (`N=366 · 4,642초`) |
| **계획서의 외경 열이 통째로 잘못 잡힘** (모델명 800·1200 이 외경으로) | 별칭 `o.d` 가 헤더 `MODEL` 에 부분일치하면 바로 옆의 정확한 `OD` 가 영영 이기지 못함 | **완전일치가 부분일치를 항상 이기도록** |
| 같은 판매오더 접미어가 **실제 `-2` 오더와 충돌**해 한 오더가 사라짐 | 무조건 `-2` 를 붙임 | 이미 쓰인 번호를 피해서 붙임 |
| **병합된 일자 셀의 행이 한 달 앞당겨짐** | 빈 일자를 「날짜 없음」으로 보고 시작일부터 6시간 사다리 생성 | 직전 행의 날짜를 이어받음 |
| 자재내역 `12192mm` 를 **12,192,000mm** 로 읽음 | 정규식 `[mM]` 이 `mm` 의 첫 글자에 걸림 | `mm` / `M` 구분 |
| 실적 본당(처리량 기준)에서 **유휴 7.6% 가 통째로 누락** | 4시간 넘는 구간을 합계에서도 제외 | 합계는 전 구간 포함, 페이스 중위수만 필터 유지 |
| 표준시간 가중치가 포장 완료 본수 기준 | 상류 설비의 WIP 본수를 반영 못 함 | 설비별 실적 본수로 가중 |
| 실적 오더셋에서 **456본 중 69본(15%) 누락** | 포장 완료 본수만 오더 크기로 사용 | 어느 설비에서든 관측된 최대치 |

#### 안전·견고성

- **프로토타입 오염** — 기준정보 JSON 에 `__proto__` 키가 있으면 `Object.prototype` 이 오염돼
  이후 모든 화면이 터지고 되돌릴 수도 없었음 → 자기 소유 키만 수용
- **업로드 CSV 로 스크립트 실행** — 실적 로그의 WO 번호와 외부 스케줄 파일명을 escape 없이
  `innerHTML` 에 넣고 있었음 (`<img onerror=…>` 로 재현) → 전부 escape
- 기준정보 불러오기가 **반쯤 적용된 채 실패**하면 되돌릴 수도 없었음 → 원자적 적용 + 형태 검증
- 분모 0·음수 상수 입력 → 완료일 146만 일 / 가동률 Infinity% → 항목별 하한 + 무한대 안전망
- 설비 대수 25·0·2.7 이 화면엔 반영된 듯 보이지만 실제로는 무시됨 → 1~20 정수 검증 후 거부 안내
- CSV 필드 중간의 `42"` 가 파일 나머지를 삼킴 → 인용은 필드 맨 앞에서만 시작
- 시각을 하나도 못 읽는 로그 파일이 예외로 죽음 → 오류 메시지 반환
- 헤더가 25행보다 아래면 파일을 아예 못 열었음 → 60행까지 탐지·선택

#### 검사 체계

- **`tools/verify_static.js` 신설** — 모든 탭을 실제로 연 뒤 문서 전체의 **id 중복**,
  소스가 참조하는 **id 존재 여부**, 콘솔 오류를 자동 점검.
  (「3D 가동률 0%」와 「설비 클릭 시 TypeError」가 이 검사로 잡혔습니다)
- `verify_audit.js` 를 **72항목**으로 확대 (기존 40 + 2차 감사 32)
- 전체 검사 5종 — `verify_formulas` · `verify_audit` · `verify_prodlog` · `verify_static` · `verify_ui`

### 2D · 3D 를 한 파일로 통합 (2026-08-14)

`JCOE_Simulator.html` 과 `JCOE_3D.html` 두 파일이던 것을 **한 파일**로 합쳤습니다.

- **엔진·xlsx 880KB 중복 제거** — 2,887KB(2개) → **1,874KB(1개)**
- **3D 가 자체 `runSim()` 을 돌리던 것을 제거.** 2D 가 계산한 `SIM` 을 `JCOE3D.update()` 로 받아
  그리기만 합니다. 종전에는 2D 에서 기준정보·계획서를 바꿔도 3D 는 옛 값이라
  **두 화면이 서로 다른 답**을 보여줄 수 있었습니다
- `scene3d.js` 를 `window.JCOE3D = (function(){…})()` 로 감싸 전역 충돌 해소
  (`SIM`·`CFG`·`$`·`fmtT`·`loop`·`nodeState`·`logs` 가 `ui.js` 와 겹쳤음)
- DOM id 충돌 24건은 3D 쪽 `$()` 에 **`v3_` 접두어**를 붙여 해소 — 3D 마크업 이름은 그대로
- 3D 탭 최초 진입 시에만 `mount()`. WebGL 불가 PC 는 3D 탭에만 안내를 띄우고 나머지는 정상 동작
- `build3d.py` · `src3d/shell3d.html` · `JCOE_3D.html` 삭제
- **통합 직후 발견한 버그 수정** — 3D 「공정별 가동률」이 전부 0% 로 나왔다.
  막대 요소 id(`sf_EM12` · `sv_EM12`)를 2D 사이드 패널도 똑같이 쓰고 있어 한 문서 안에서 id 가
  중복됐고, 3D 쪽 `$()` 가 자기 막대를 찾지 못했다. 생성되는 막대 id 에도 `v3_` 접두어를 붙여 해결
- `verify_ui.js` 가 「2D 에서 3근 전환 → 3D 도 16.4일」 일치와 **3D 가동률 막대가 실제로 값을 보이는지**를 매번 확인

### 기준정보 탭 신설 — 현장에서 코드·빌드 없이 숫자 수정 (2026-08-14)

표준시간 상수 78개가 `engine.js` 함수 안에 숫자로 박혀 있어 현장에서 고칠 수 없었다.
전부 `REF_STD_DEFAULT` 로 빼내고 「기준정보」 탭에서 편집하도록 했다.

- **설비 대수**(28) · **전환시간**(15공정×3) · **표준시간 상수**(15공정 78항목) 화면 편집
- 고치면 **즉시 재시뮬** → 완료까지 / 1위 병목 / 원래 대비 ±일 이 바로 갱신
- 원본과 다른 칸은 노란색 + 칸별 되돌리기(↺), 「전부 원래대로」 버튼
- 공정마다 **미리보기**(OD914×t9.3×12.802m)와 **엑셀 행 번호** 병기
- **JSON 내보내기/불러오기** — 기본값과 다른 항목만, 한글 키. 왕복 복원 검증 완료
- `setRefStd({})` 가 빈 객체를 truthy 로 흘려 되돌리기가 안 먹던 버그 수정
  (항상 기본값으로 초기화한 뒤 패치를 얹도록 변경)
- 값을 안 건드리면 엑셀 산출식과 완전히 동일 — `verify_formulas.js` 전 항목 PASS 유지
- 회귀 검사 `verify_audit.js` 에 기준정보 C1~C6 추가 (총 40항목)

### 세아제강 확인 요청 전건 종결 (2026-08-14 2차 회신)

- **20251218 엑셀** — 읽을 수 있는 표는 이미 전부 반영돼 있음을 확인 (#1호기 107행 · R/B 37행 ·
  산출식 20개 · N 산출식 이미지). `Expander(2호기)` 시트만 이미지라 엑셀 원본 대조 불가이나
  2026-08-06 「specs.py 가 맞습니다」 회신으로 확정 → **추가 요청 없음**
- **모관검사 IP106** — 표준시간 엑셀 20개 공정에 산출식 없음을 확인.
  세아제강 확정대로 **공정 흐름에 넣지 않고** 실적 검증 탭에 표시만 유지
- **R/B 라인** — 「거의 사용 안 함」 확정. 실적 로그의 EP102 0건과 일치.
  기본값(열처리·배척만)에서 R/B 0본으로 현장과 일치. 「적격 제품 전량」 모드는
  What-if 시나리오임을 화면에 경고하고 선택지 순서를 뒤로 이동

### 전수 감사 — 계산 오류 10건 정정 (2026-08-14)

표준시간 산출식 20개의 골격·상수는 **전부 엑셀과 일치**했고, 결함은 조건 분기·빠진 항·룩업 경계에 있었습니다.

| # | 증상 | 정정 |
|---|---|---|
| A1 | **Gap Press 가 실제의 2~4배** — 엑셀 대괄호 밖 ×2 가 「X70 이상」 조건인데 무조건 곱한 뒤 또 ×2. 투입조건(t>25)과 고강도 대리변수(t>25)가 같아 늘 4배 | 1,981s → **1,201s** (본당 −780s) |
| A2 | Edge Miller 「첫 본 +1분 50초」 누락 | 오더 첫 본 +110s |
| A3 | 수압 「36" 이상」을 짝수 스냅 값으로 판정 → 35"(OD889)가 36" 상수 | 745s → **565s** |
| A4 | R/B 열처리 상수 219s (엑셀에 없는 값) | 「15초 제외」 문자 그대로 → **234s 고정** |
| B1 | **R/B 길이 상한 12.8 하드코딩** → 주력 12.802m 제품 960/1,446본(66%)이 R/B 부적격 | `L2 = 12.8384` 적용 |
| B2 | 같은 판매오더 다중 행에서 `orderSpan` 덮어쓰기 → **가동률 100% 초과** | 합산 |
| B3 | 로더의 `-2` 접미사 때문에 R/U 분할 오더가 핀되지 않음 | `planLookup` 폴백 |
| B4 | **EP102→M2 / EP103→M3 / EP104→M1** 오매핑 | 명시 표로 정정 |
| B5 | 포장 1/10 추가검사가 오더 쪼갠 방식에 의존 | 전역 순번 사용 |
| B6 | 외부 스케줄의 R/B 지정이 적격성 무시 | 검증 후 폴백 + 경고 |
| B7~B12 | 라우트 상한 초과 본 소멸 · sheet 모드 납기 분석 무효 · 보정이 최적화에 미적용 · C2 제품 R/B 미강제 · R/B 2차 U.T 무조건 · 빈 오더셋 −Infinity | 전부 정정 |

정정 후 기본 시뮬 25.0일 → **24.4일**, 가동률 100% 초과 없음, 미완주 0본.
회귀 검사 `tools/verify_audit.js` 34항목 신설.

### 회귀 검사

`node source/tools/verify_formulas.js` 전 항목 PASS (엑셀 예시 `24"×12.7t → 33회`,
`RB StepSize = 다이−90`, `RB N=21`, `RB 519s` 추가).
`node source/tools/verify_prodlog.js` 전 항목 PASS. `node source/tools/verify_ui.js` 2D·3D 오류 0.

---

## 2차 전수 감사 — 계산 오류 12건 수정 + 확인요청서 재정리

캘린더 재작성·R/B 라인 신설·공구 폴백·로더 확장이 들어간 뒤 감사받지 않은 코드가 많아
네 갈래(신규 경로 / 집계·화면 일관성 / 실계획서 로더 / 문서 수치)로 다시 훑었습니다.
전부 `node`·헤드리스 브라우저로 재현을 확인한 뒤 고쳤습니다.

### A. 계산이 틀렸던 것

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| A1 | **한국 시간대에서 계획일·납기가 전부 하루 밀림** | SheetJS `cellDates` 가 1899-12-30 을 로컬 기준으로 잡는데, 서울은 당시 LMT +8:27:52 라 52초 어긋나 **전날 23:59** 로 떨어짐. 08:00 정규화도 건너뜀 | `toDate` 가 자정 ±2분을 달력일로 스냅. 엑셀 serial 도 로컬 자정으로 직접 생성. **UTC·서울·뉴욕 세 시간대에서 결과 동일** 확인 |
| A2 | **실가동 h/Shift 민감도 분석이 무효** | 각 교대의 마지막 구간만 늘리는데, 1근 끝(15:00)이 2근 시작(15:00)과 붙어 있어 늘린 만큼이 병합에 흡수. **3근은 아예 무반응**(7.5→10h 로 바꿔도 22.5h 고정) | 하루 가동창 전체를 비례 확대·축소. **교대 4종 × 실가동 9종 = 36개 조합 전부 정확** |
| A3 | 작업 소요가 가동창 잔여와 **정확히 같으면 다음 창으로 통째로 밀림** | `avail >= left` 부동소수 비교. 10분 작업이 최대 **+64시간**(금요일 + 주말 비가동) 밀림 | 허용오차 1e-6 |
| A4 | **좌측 가동률 게이지가 병렬 설비를 대수만큼 부풀림** | 전 호기 합산을 1대 용량으로 나눔. 슬러그 4대 → **4배**. 내면 SAW·F-X ray 가 항상 100% 로 포화돼 보임 | 호기별로 적산해 **가장 바쁜 1대** 기준 |
| A5 | 게이지에서 R/B 후속 3노드가 **절반으로 표시** | `'RB'` 문자열 하나만 1근 캘린더 특례. `RBEF`·`RBRT`·`PACKRB` 누락 | R/B 라인 4개 전부 1근 분모 |
| A6 | **마감 달성률·포장 완료 카운터가 R/B 물량 누락** | `nid==='PACK'` 만 셈 | `PACKRB` 포함. 증산 시나리오에서 달성률 4.3%p 과대였음 |
| A7 | **R/B 물량은 불량이 발생하지 않음** | 불량 트리거가 `nid==='FX'` 하드코딩. R/B 검사는 `RBRT`(RT104) | `FX` 또는 `RBRT`. 재작업 경로의 `indexOf(-1) → slice(-1)` 도 방어 |
| A8 | 폴백 매핑 시 **같은 물리 다이인데 다이 교체 90분** | `die` 식별자만 입력 외경을 씀. OD457·OD500 이 둘 다 다이표 610 으로 가는데 키가 다름 | 폴백 구간에서는 매칭된 다이표 외경을 키로 |
| A9 | 병목 표 **한 줄 안에서 검산이 안 됨** | 가공·전환은 표시값인데 전환 비중만 다른 기준(호기 점유) | 표시값과 같은 조합으로 통일 (확관 18.1% → **16.9%**) |
| A10 | 병목탭 「유닛별 부하」 카드만 전환시간 제외 | `u.busyH` 만 표시 | `busyH + setupH`. OPT 에서 233.6h vs 194.2h(39.4h 차)로 보이던 것이 245.1 vs 245.7(0.6h)로 정정 |
| A11 | 화면 분류가 시뮬과 다름 | 「제약 기준 비교」·「단일 호기 전용」·「대표 규격 CT」가 **절사 전 원본 외경**으로 재판정 | 전부 `specOf` 를 통과시켜 시뮬과 같은 정규화 적용 |
| A12 | 계획서 스킵 안내의 **행번호가 엑셀 행번호와 다름** | 헤더 오프셋 미반영 | `rowBase` 적용 (3행 → 4행) |

### B. 조용히 잘못 보이던 것

- **3D 뷰에서 R/B 후처리 3개 설비가 Expander 와 완전히 겹침** — `POS3` 좌표 누락으로 폴백 `[3,3]`(=EXP). R/B 전용 행(row 6)을 신설했습니다.
- **설정을 바꿔도 「규칙별 비교표」·「반복 실행」 결과가 남음** — 2근→1근으로 바꾼 뒤에도 비교표가 옛 조건의 36.5일을 "현재 값"처럼 표시(실제 73.2일). `invalidatePlan` 에서 함께 초기화.
- **위저드에 epoch 초 노출** — `마감 1774483199 내 달성률`. 날짜 문자열로 수정.
- **전환시간을 끄면 「JCOE 포장 — 전환 병목 0.0h」** — 값이 0 이면 지목하지 않도록 가드.
- **2근연장을 UI 에서 고를 수 없었음** — 엔진은 지원(18.5h)하는데 셀렉트에 항목이 없고, 있어도 `+'2E'`→NaN 으로 2근 강등. 선택지 추가 + 문자열 허용.
- **API 5CT 를 API 5L 로 오판** — `/^API/` 가 `API-J55-DOM`·`API-N80X` 같은 케이싱·튜빙 등급까지 잡음. 현재는 전부 ERW 행이라 미도달이지만 배제 규칙 추가.
- **계획서 열 지정 UI 에서 재질·용도·물종을 바꿀 수 없었음** — 물종은 **행을 스킵시키는** 필드인데 해제 수단이 없어, 표기가 다른 계획서가 오면 전량 스킵되고 복구 불가였습니다.

### C. 문서 수치 전면 갱신

README 의 수치 20여 곳이 `rbMode` 기본값 변경(`capable`→`force`)과 실제 교대창 도입 **이전 값**이었습니다.
몬테카를로 분포, 공구 계층 절감률, 파일 크기, SA 소요, 병목 표, 정본/구버전 비교 표를 전부 재실행해 갱신했습니다.
사실과 달랐던 서술도 고쳤습니다 — “N 은 항상 홀수”(→ 정본은 짝수 보정), “2호기 다이표는 1호기 공용”(→ 68행 별도),
“공구 미상으로 표시”(→ 최근접 폴백), “RB 투입 조건 외경 24" 이하”(→ 다이표 24~48"), `ortools_final_v3`(→ `v2_ep12`).

### D. 확인요청서 재정리

해결된 항목을 본문에서 걷어내고 **아직 답이 필요한 5건만** 앞으로 뺐습니다.

1. `die_specs_m2` 68행의 출처 (엑셀 `Expander(2호기)` 시트가 비어 있음)
2. 다이표 중복·모호 항목 실무 확인 결과
3. **R/B 라인에 API 5L 제품을 보내도 되는지** (R/B 에는 수압·2차 U.T 가 없음)
4. **납기 열이 `영업계획납기일` 이 맞는지** (1월 계획서는 142건 중 123건이 계획 시작 전 납기)
5. 실적 데이터

### 검증

`node source/tools/verify_formulas.js` 45항목 전부 PASS ·
`node source/tools/verify_ui.js` 2D/3D 콘솔 오류 0 ·
교대×실가동 36개 조합 dayCap 정확 · 실계획서 2종(2025-12 · 2026-01)이 pandas 집계와 완전 일치
(202오더 1,003본 / 142오더 1,980본).

### 기준값 (변동 없음)

| 규칙 | Makespan | 확관 전환 | #1/#2 | 편차 | 1위 병목 |
|---|---:|---:|---:|---:|---|
| EAT | 32.5일 | 87.0h | 839/957 | 6.3h | JCOE 포장 74.1% |
| SETUP | 33.1일 | 58.0h | 908/888 | 65.9h | JCOE 포장 73.3% |
| **OPT** | **25.1일** | 54.5h | 911/885 | 0.6h | JCOE 포장 **96.8%** |

---

## 다이 스펙표 엑셀 대조 + 공구 매핑 최근접 폴백

### 1. 엑셀 `Expander(1호기)` 시트와 `specs.py` 대조 — 107행 전부 일치

“툴 스펙은 표준시간 분석 엑셀의 Expander 페이지에 있다”는 회신을 받고, 엑셀에서 표를 직접 뽑아
인치 → mm 환산 후 `die_specs_m1` 과 기계적으로 비교했습니다.

| | 행수 | 차이 |
|---|---:|---|
| 엑셀 `Expander(1호기)` | 107 | — |
| `specs.py` `die_specs_m1` | 107 | **0건** |

`specs.py` 의 1호기 다이표는 엑셀을 정확히 옮긴 것입니다.

> **`Expander(2호기)` 시트는 비어 있습니다** (966×62 이지만 값이 있는 셀 0개).
> `die_specs_m2` 68행은 **엑셀에 근거가 없고 `specs.py` 에만 존재**합니다 — 출처 확인 문항으로 등록했습니다.

### 2. “다이표에 없는 규격 사이의 셋업이 0초” 의 정체

되물어 주신 항목입니다. **두께는 최근접 매핑이지만 외경은 ±5mm 하드 컷**이었습니다.

```python
candidates = [k for k in d_dict.keys() if abs(k - diameter) <= 5]
if candidates: ...
else:          continue        # 두께를 볼 기회조차 없이 그룹을 건너뜀
if not found_any: return (None, None, ("UNKNOWN", "UNKNOWN_DIE"))
```

UNKNOWN 끼리는 head·drawbar·die 가 모두 같다고 판정되어 **서로 완전히 다른 규격 사이에도 셋업 0분**이 됩니다.
R/B 다이표는 외경 610~1219 만 있어서, R/B 에서 OD457 → OD914 전환이 0분으로 계산되고 있었습니다.

### 3. 공구 매핑에 최근접 외경 폴백 추가

> “로직에는 해당 오더의 스펙과 가장 가까운 tool 에 매핑되도록 해놓았습니다.”
> “job creator 의 규칙도 근사값을 사용하도록 수정하면 좋겠습니다. RB 라인으로 빼는 규칙 자체가 soft 한 면이 있습니다.”

- `toolInfo` — 외경 ±5mm 안에 후보가 없으면 **가장 가까운 외경**의 공구로 폴백합니다.
  `OD457(R/B) → 다이표 610 · 헤드 #600 · 9t(550mm)`, 화면에 `외경 457 → 다이표 610 (153mm 차)` 표시.
  UNKNOWN 분기가 사라져 드로바 270 / 헤드 150 / 다이 90분이 제대로 판정됩니다.
- R/B 적격 판정은 계속 **외경 ±5mm 근사**를 씁니다(정확일치 아님) — 말씀하신 soft 한 취지와 같습니다.

### 4. 그대로 둔 것

| 항목 | 처리 |
|---|---|
| OD1219 `44t(250mm)` / `44t(700mm)` 중복 | 엑셀 원본에도 중복. 엑셀 순서상 250 이 먼저 → **250 채택**을 회귀 검사로 고정 |
| OD914 `18.6~38.1t(340mm)` 구간 포함 | 실제 데이터가 그러함. 원본 그대로 유지 |
| 1호기 산출식 | **ST 수정 중이므로 전달받은 파일 그대로 고정.** 분모 하한 없음 유지 |

### 검증

`node source/tools/verify_formulas.js` 전 항목 PASS —
폴백(OD457 → step 550 · 헤드 600), OD1219 t44 → 250 을 회귀 검사에 추가했습니다.
`specs.py` 재대조 — #1·#2호기는 여전히 **전건 일치**, R/B 의 다이표 미등재 외경만 의도적으로 다릅니다.

---

## 현장 회신 반영 — 설비 대수 · R/B 전용 후처리 라인 · 실제 교대 시간표

2026-08-06 세아제강 회신과 현장 설비 화면(Zone 2 확관/후처리 · Zone 3 R/B & 열처리)을 반영했습니다.
**병목 진단이 확정되고, 공정도가 바뀝니다.**

### 1. 슬러그 제거 1대 → 4대 — 2위 병목이 사라짐

> “내면 SAW / 슬러그 제거 / 외면 SAW 전부 4개씩 있습니다. 한 라인에 내면–슬러그제거–외면이 이어지고 다시 합쳐진다고 생각”

| | 종전 | **확정** |
|---|---:|---:|
| 슬러그 제거 가동률 | 61.3% (**2위 병목**) | **15.3%** (하위권) |
| 1위 병목 | JCOE 포장 74.6% | JCOE 포장 **74.1%** |
| 2위 그룹 | 슬러그 61.3% | 면취 53.8% · 확관 53.8% · 수압 53.6% |

1위와 2위 격차가 **20%p 이상** 벌어져, **“포장 및 출하가 병목”이라는 진단이 확정**되었습니다.
그동안 이 항목을 「★최우선 확인」으로 남겨 두었던 이유가 해소됐습니다.

### 2. R/B 라인 — 후처리 설비가 완전히 분리되어 있었음

설비 화면에서 설비 코드까지 확인했습니다.

```
JCOE 본류  1차U/T UT109 → 10번 문 → Expander EP103·EP104 → 면취 FC110 → 수압 HY106
           → 2차U.T UT110 → 관단R/T RT102 → F-X ray RT101(320kV)·RT105(450kV) → JCOE 포장 PK113
R/B 라인   R/B Expander EP102 → R/B 면취 FC112 → R/B RT RT104 → 배척 포장 PK112
```

- **R/B 라인에는 수압 Test 와 2차 U.T 가 없습니다.**
- 종전에는 `RB → 포장 직행` 이라 **JCOE 포장(PK113) 용량을 잘못 나눠 쓰고** 있었습니다.
  전용 노드 3개(`RBEF` · `RBRT` · `PACKRB`)를 신설해 분리했습니다.
- **R/B 기본 투입 규칙 변경** — “#1·#2호기의 잔여 CAPA 가 없을 때 생산량 증대를 위해 가동시키는 여분 라인”.
  기본값을 「적격 제품 전량 R/B」 → **「열처리·배척 제품만」** 으로 바꿨습니다.
  증산 시나리오는 셀렉트에서 「적격 제품 전량」으로 볼 수 있습니다.
- **확관 3호기 = R/B 라인 확정** (“설명을 쉽게 드리려고 3호기라고 말씀드렸다”). 별도 설비 토글을 제거했습니다.
- 설비 화면의 **옥외 열처리 HT102** 가 계획서 `병목 공정 작업장 = HT102` 와 대응함을 확인했습니다.
- 모든 공정 노드에 **실제 설비 코드**를 붙였습니다.

### 3. 실제 교대 시간표 반영 — 종전 대비 최대 1시간 어긋나 있었음

| 교대 | 작업 구간 | 휴식 | 실가동 |
|---|---|---|---|
| 1근 | 07:00–11:30, 12:00–15:00 | 점심 11:30–12:00 | 7.5h |
| 2근 | 15:00–18:00, 18:30–23:00 | 저녁 18:00–18:30 | 7.5h |
| 3근 | 23:00–02:45, 03:15–07:00(익일) | 야식 02:45–03:15 | 7.5h |
| 2근연장 | (2근에 추가) 23:30–03:00(익일) | — | +3.5h |

종전에는 `08:00–15:30 / 16:00–23:30` 으로 잡았습니다. 2근 기준 실제 가동창은
`07:00–11:30 · 12:00–18:00 · 18:30–23:00` (하루 15.0h) 입니다.
**근무 형태가 월마다 달라 2근 고정**으로 진행합니다.
`실가동 h/Shift` 를 7.5 에서 바꾸면 각 교대의 마지막 구간이 그만큼 늘거나 줄어듭니다(민감도 분석용).

### 4. 그 밖

- **계획서 `RunHrs` 는 무시해도 되는 값** — 직전 항목에서 “우리 산출의 2배”로 올렸던 문항을 내렸습니다.
- **셋업은 상위값만 적용하는 것이 맞음** — 헤드+다이 동시 교체 시 150분(합산 아님). 현재 구현대로입니다.
- **불량률·보수 소요는 일단 무시** — 변동성 시뮬레이션은 꺼진 기본값을 유지합니다.

### 새 기준값 (정본 · 2근 · 실제 교대창 · R/B 열처리·배척만)

| 규칙 | Makespan | 확관 전환 | #1/#2 | 부하 편차 | 1위 병목 |
|---|---:|---:|---:|---:|---|
| EAT | 32.5일 | 87.0h | 839/957 | 6.3h | JCOE 포장 74% |
| SETUP | 33.1일 | 58.0h | 908/888 | 65.9h | JCOE 포장 73% |
| **OPT** | **25.1일** | 54.5h | 911/885 | 0.6h | JCOE 포장 **97%** |

R/B 로 빠지던 93본이 확관으로 돌아와 확관 전환이 69.5 → 87.0h 로 늘었습니다.
최적화를 걸면 **25.1일(−23%)** 까지 줄지만 그때는 **포장이 97%로 완전히 포화**됩니다 —
확관을 아무리 잘 돌려도 포장 1대가 상한이라는 뜻입니다.

### 변경 파일

```
source/src/flow.js     SHIFT_BLOCKS/foldWindows(실제 교대창) · SLUG cap 4 · RBEF/RBRT/PACKRB 노드
                       routeOf 의 R/B 분기 · RB_LINE 캘린더 · rbMode 기본값 'force' · 설비 코드 라벨
source/src/shell.html  R/B 투입 셀렉트 재정의 · 확관 3호기 토글 제거 · 교대 시간표 안내
source/src/ui.js       useM3 고정 false · rbMode 기본값
source/tools/verify_ui.js  3호기 토글 시나리오 → R/B 라인 시나리오로 교체
```

---

## 실제 조관계획서 76열 반영 — 대리변수 3개 제거

`ortools_final_v2_ep12\data\` 안에 **세아제강 실제 조관계획서**(2025-12 · 2026-01)와 자재 조회 파일이
들어 있었습니다. 열어보니 그동안 "계획서에 없다"고 가정하고 대리변수로 때우던 값들이 **전부 있었습니다.**

### 계획서 JCOE 시트 = 76열

| 읽게 된 열 | 값 | 대체한 대리변수 |
|---|---|---|
| **`용도`** | 송유관 28 / 압력배관용 168 / 구조용 6 | `수량 50본 이상 = API 5L` |
| **`재질`** | `API-X80L2*CESAG` · `A516-60N*TAZ` · `A572-50` · `SS400` … 14종 | `두께 25T 초과 = 고강도` |
| **`영업계획납기일`** | 전 행 | 납기 미적용 |
| **`물종`** | JCOE 202 / **ERW 2** | (JCOE 시트에 ERW 가 섞여 있는 줄 몰랐음) |
| **`계획수량`** | 수주수량·오더수량·계획수량·조관수량·포장수량 **5종 중 하나** | `수량` 키워드 매칭 |

`용도 = 송유관` 28건과 `재질`이 `API-` 로 시작하는 28건이 **정확히 일치**합니다.
API 5L 판정은 이제 대리변수 없이 확정됩니다.

### 이 과정에서 드러난 로더 버그 2건

| 증상 | 원인 | 영향 |
|---|---|---|
| **수량을 52% 과대 인식** | `KEYS.qty` 1순위가 `수량` 이라, 더 왼쪽에 있는 **`수주수량`** 이 먼저 잡혔다. 운영 모델(`data_loader.py:22`)이 쓰는 것은 `계획수량` | 12월 계획서 **1,522본 → 1,003본** |
| ERW 제품이 JCOE 로 계산됨 | `물종` 열을 읽지 않음 | 2건 제외 |

### 검증 — 실제 12월 계획서

로더 결과가 pandas 로 직접 센 값과 정확히 일치합니다.

```
202오더 / 1,003본 (계획수량 합계 1,003 · JCOE 행 202 와 일치)
API 5L 30건 · 고강도(X70↑) 10건 · 납기 202건 · 병목공정 173건 · 원재료내역 202건
ERW 2건 제외
```

시뮬레이션도 끝까지 돌아갑니다 — EAT 23.6일 / OPT 23.3일, **납기 202건 중 11건 지연**.
Final U.T·관단 X-ray 물량이 수량 대리변수 기준 814본에서 **API 5L 기준 180본**으로 바뀌었습니다.

### 남은 것

`RunHrs` · `병목 공정 RunHrs` 열이 계획서에 있는데 **우리 산출값의 약 2배**입니다.
게다가 이 값은 **길이에만 반응**하고 외경·두께에는 반응하지 않아, 다이 step 으로 확관 횟수를 정하는
`specs.py` 와 계산 구조가 다릅니다. `세아제강_확인요청.md` ② 로 등록했습니다.

### 변경 파일

```
source/src/planload.js   KEYS.mat/use/kind 추가 · qty 우선순위(계획수량 1순위) · isApi5L/gradeOf · 물종 필터
source/src/flow.js       specOf 주석 (재질 열 우선, 두께는 폴백)
세아제강_확인요청.md        ② API 5L · ⑦ 재질 · 납기 → 해결 처리, RunHrs 문항 신설
```

---

## 전수 감사 — 계산 오류 9건 + XSS 1건 수정

`ortools_final_v2_ep12` 원본 4개 파일(`specs.py` · `data_loader.py` · `job_creator.py` · `optimizer_grouped.py`)과
대조하고, 시뮬레이터 자체도 계산·상태 관점에서 전수 감사했습니다. 지적은 전부 `node`/헤드리스 브라우저로 재현을 확인한 뒤 고쳤습니다.

### A. 정본 대조에서 나온 것

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| A1 | **OD 711.0 ↔ 711.2 사이에 헛된 다이 교체 90분** | `data_loader.py:51-52` 가 계획서를 읽을 때 **외경을 정수로 절사**(`astype(int)`)하고 두께를 소수 2자리로 반올림하는 것을 놓쳤다. 직전 커밋에서 `die_id` 를 입력 외경 기준으로 바꿨는데, 그 "입력 외경"이 이미 정수라는 전제가 빠져 있었다 | `specOf` 에서 `od=Math.trunc(od)` · `t=round2(t)` 적용. RB 적격의 외경 정확일치도 이제 정본과 같은 답 |
| A2 | 다이어그램에서 조건이 서로 다른 **D2(1차 U.T)와 D5(Final U.T·관단 X-ray)가 같은 조건**이 됨 | 계획서에 API 5L 열이 없어 `qty >= 50` 을 대리변수로 쓰는데, 그 값이 D5 에도 그대로 흘러들어감. 814본(전체 56%)이 근거 없이 Final U.T·관단 X-ray 를 탐 | 「API 5L 판정」 셀렉트 신설. **기본값은 종전 동작 유지**(수치 변화 없음), 끄면 Final U.T·관단 X-ray 0본. 세아제강 확인 문항으로 등록 |

### B. 시뮬레이터 자체 감사에서 나온 것

| # | 증상 | 재현 | 수정 |
|---|---|---|---|
| B1 | **「호기 부하 편차」가 화면 두 곳에서 다른 값** | ③ 최적화 패널은 5.5h, 위저드·반복실행·규칙비교는 8.0h | KPI 도 최적화 목적항과 같이 **가공+전환** 기준으로 통일. **결론이 바뀝니다** — 아래 참조 |
| B2 | 동시가동(BOTH) **전환시간 이중 계상** | 한 번의 셋업이 두 호기 누산기에 각각 더해짐. 실제 벽시계 69.5h ↔ 표기 77.0h (**+10.8%**) | 노드 전환시간은 벽시계, 호기별 가동률은 점유 기준으로 분리 |
| B3 | **면취기 룩업 구멍** — 두께 8mm 미만이면 표의 마지막 행(64" · t30~999 · 최악값) 채택 | OD508 t7.0 → 880.1s / t8.0 → 506.6s. **0.1mm 차이에 +74% 점프** | 가장 가까운 두께 구간 사용. t7.0 → 506.6s |
| B4 | **오더번호 XSS** — 계획서 엑셀의 오더번호가 이스케이프 없이 HTML 로 | `<img src=q onerror=...>` 를 오더번호로 넣은 계획서에서 **스크립트 실행 확인** | In/Out 표·간트 라벨·`title` 속성·최적화 시퀀스 등 6곳에 `esc()` 적용. 재현 시 실행 안 됨 확인 |
| B5 | **확관 N·셋업 산출식을 바꿔도 최적화 해가 무효화되지 않음** | N 식을 바꾼 뒤 ③ 패널이 옛 값(Cmax 235.7h)을 그대로 표시 — 실제와 34% 차이 | `invalidatePlan` 대상에 `cfgExpN`·`cfgExpSetup`·`cfgApi5l` 추가. `IMPORT` 규칙도 되돌림 |
| B6 | **좌측 가동률 게이지가 병목 탭과 다른 값** | 전환시간 제외 + RB 에도 M1/M2 캘린더 적용 + 대수 평균. RB 10%(실제 19.7%), EXP 23%(병목 탭 51%) | 병목 탭과 정의 통일 — 전환 포함 · 설비별 캘린더 · 호기 1대 기준 |
| B7 | Edge Miller 전환 판정만 `t >= 25` 로 남음 | t=25.0 제품이 Gap Press 는 안 타는데 Edge Miller 에서만 "25T 초과군". EM12 전환 5.5h → 4.5h | `t > 25` 로 통일 |
| B8 | 외부 CP-SAT 스케줄 **Makespan 60배 표기** | 마지막 행 `en` 하나로 초/분 판정. `en=null` 이거나 makespan ≤ 20,000초면 오판 → 0.2일이 12.5일로 | 전 행 최댓값으로 판정 |
| B9 | 계산기에서 길이를 비우면 **그럴듯한 오답** | "1본 1.55h · 확관 N 0회 · 병목 포장" 이 크래시 없이 출력 | 시뮬 설정과 같은 범위 검증 적용 |
| B10 | 계획서를 갈아끼워도 **이전 계획서의 반복 실행 분포가 남음** | `applyOrders` 가 `mcSum/mcHist/mcTable/mcRules` 를 안 지움 | 함께 초기화 |

### ★ 결론이 바뀌는 것 — 호기 부하 편차

B1·B2 를 고치면 **"최적화로 호기 부하 편차가 줄어든다"** 는 종전 서술이 성립하지 않습니다.

| (정본 기준) | Makespan | 확관 전환 | 호기 부하 편차 |
|---|---:|---:|---:|
| EAT | 32.4일 | 69.5h | **3.6h** |
| RR | 32.4일 | 69.5h | 8.8h |
| SETUP | 32.4일 | 47.5h | 38.7h |
| SPEC | 32.4일 | 69.5h | 3.6h |
| **OPT** | **30.4일** | **42.0h** | 5.5h |

정본 기준에서는 **어느 규칙을 써도 두 호기 부하가 전체 230h 대비 2~4% 안쪽**입니다.
최적화의 실질 성과는 **Makespan −6% · 확관 전환 −40%** 이고, 부하 편차는 이미 균형이라 개선 여지가 없습니다.
(SETUP 규칙만 38.7h 로 튀는데, 같은 규격을 한 호기에 몰아주는 규칙의 성격상 당연합니다.)

### 정본과 다르지만 **모델 범위 차이**로 남긴 것

운영 모델은 자재재고·WIP 를 입력으로 받는 스케줄러이고, 시뮬레이터는 계획서만 받는 공정 모델입니다.
아래는 "버그"가 아니라 **입력이 없어 재현할 수 없는 것**이라 문서로만 남깁니다.

- **Ready / Unready 분할** — 정본은 자재 확보분(R)·미확보분(U)을 별도 job 으로 쪼개고, 같은 오더의 R/U 를 같은 호기에 강제하며, Priority 티어(0 긴급 ~ 4 Unready)를 **하드 제약**으로 겁니다. 시뮬레이터에는 자재 입력이 없어 전량 Ready 로 봅니다.
- **CAPA 캡** — 정본은 `max_total_jobs=350` 등으로 투입량을 자릅니다. 시뮬레이터는 계획서 전량을 흘립니다.
- **외경 그룹 연속배치 하드 제약 · Fixed_Machine 핀** — 정본 CP-SAT 에만 있습니다.
- **목적함수** — 정본은 `makespan×1e6 + 긴급완료합×1e5 + W오더완료합×5e4 + Σstart`. 시뮬레이터의 SA 는 `w₁·Makespan + w₂·Σ전환 + w₃·부하편차` 로, **긴급도 대신 전환시간을 직접 목적에 넣은 다른 모델**입니다. 계획서에 긴급 열이 오면 반영을 검토하겠습니다.
- **오더 원자성** — 정본은 오더 1건 = 구간 1개(호기 분할 불가). 시뮬레이터는 본 단위라 39개 확관 오더 중 21개가 두 호기에 걸칩니다. 공정 흐름을 보는 목적에는 본 단위가 맞지만, CP-SAT 결과와 나란히 볼 때는 다른 모델임을 감안해야 합니다.
- **첫 작업 셋업** — 정본은 각 호기에 610×25.4 가 물려 있다고 보고 첫 셋업(최대 270분)을 뭅니다. 시뮬레이터는 0 입니다. 3개 라인 합계 최대 11.5h 과소.
- **RB 배정** — 정본에서 RB 는 solver 가 M1/M2 와 함께 고르는 **선택지**입니다. 시뮬레이터는 「적격 전량 RB」/「Force_RB 만」/「미사용」 세 모드뿐이라 어느 것도 정본을 그대로 재현하지 못합니다.
- **RB 통과분이 면취·수압·검사를 건너뜀** — 라우팅상 RB → 포장 직행입니다(93본). 다이어그램 확인이 필요해 문항으로 등록했습니다.

### 검증

`node source/tools/verify_formulas.js` 전 항목 PASS ·
`node source/tools/verify_ui.js` 2D/3D 콘솔 오류 0 ·
정본 재대조 — 오더 58건 × 3호기 step·소요·공구 **전건 일치**, 셋업 6,075쌍 중 **불일치 0건**
(다이표 미등재 규격 498쌍은 의도적 차이).

---

## 운영 모델 `specs.py` 원본 대조 — 산출 오류 3건 수정

`ortools_final_v2_ep12` 원본을 받아 **직접 실행해 전량 비교**했습니다.
그동안 코드에 들어 있던 값은 이전 세션에서 원본을 보고 옮겨 적은 것이라, 정본으로 확정된 이상
한 번은 기계적으로 맞춰봐야 했습니다.

### 대조 결과

| 대조 항목 | 결과 |
|---|---|
| 다이 스펙표 M1 107행 + M2 68행 + RB 37행 = **212행** | **전 행 일치** (헤드그룹·외경·두께구간·step·라벨) |
| #1·#2호기 셋업 시간 45규격 × 45규격 × 2호기 = **4,050쌍** | 수정 후 **전 쌍 일치** |
| 오더 58건 × 3호기 step · 소요시간 · 헤드 · 드로바 | #1·#2호기 **전건 일치** |
| `BOTH_LENGTH_THRESHOLD` 14.021 · #2호기 12.8384 · 외경 48"↑/22"↓ 하드 제약 | 일치 |
| RB 적격 (다이표 외경 & 9≤t≤25.4 & L≤12.8) · Force_RB (HT102 · 길이비 1.8) | 일치 |
| 셋업 4단계 270 / 150 / 90 / 0분 · RB `234+(ceil(L/step)−2)×15` | 일치 |

### 고친 것 3건

| # | 증상 | 원인 | 수정 |
|---|---|---|---|
| 1 | **OD914 t18.8 에서 확관 N 이 25 → 39 회로 과대** | `specs.get_tool_info` 는 단일 두께 표기에 `math.isclose(abs_tol=0.1)` 을 써서 t18.8 ↔ `18.9t` 를 **일치(차이 0)** 로 본다. 이 규칙이 없어 차이 0.1 로 계산되면서 구간 표기 `18.6~38.1t(340mm)` 가 대신 선택됨 | `toolInfo` 에 ±0.1mm 규칙 추가. step 340 → **580** |
| 2 | 같은 다이를 쓰는 **OD 711.0 ↔ 711.2 사이 셋업이 0분** | `specs.py` 의 `die_id = (diameter, spec)` 는 매칭된 표의 외경이 아니라 **입력 외경**을 쓴다 | `die` 키를 입력 외경 기준으로. 해당 전환에 **다이 교체 90분** 계상 |
| 3 | **#1호기 N 이 과소** — OD508 t9.5(step 170) 에서 230회 | 종전에 분모(`step−150`)가 작아질 때 N 이 폭발하는 것을 막으려 **하한 50** 을 두었는데, 정본에는 그런 하한이 없다 | 하한 제거. 분모 20 → **N=575**, 소요 2,987s → **7,127s** |

수정 3건이 서로 상쇄돼 총량 변화는 크지 않지만, 개별 규격의 소요·전환은 눈에 띄게 달라집니다.

| (EAT · 정본 기준) | 대조 전 | **대조 후** |
|---|---:|---:|
| Makespan | 32.4일 | 32.4일 |
| 확관 전환 | 75.5h | **77.0h** |
| #1 / #2 처리 본수 | 802 / 901 | **804 / 899** |
| 호기 부하 편차 | 20.6h | **21.6h** |
| 최적화(OPT) | 29.5일 · 47.5h · 8.1h | **30.4일 · 52.5h · 8.0h** |

### 의도적으로 다르게 둔 것 1건

`specs.py` 는 다이표에 없는 규격을 모두 `(None, None, ("UNKNOWN","UNKNOWN_DIE"))` 로 반환합니다.
그 결과 **서로 완전히 다른 규격 사이에서도 셋업이 0초**가 됩니다. 시뮬레이터는 규격이 다르면
최소 다이 교체(90분)를 보수적으로 부과하고 「공구 미상」으로 표시합니다.
현재 오더셋에서는 RB 호기의 OD 457 · 508 만 해당하고, 두 규격 모두 RB 적격이 아니라
시뮬레이션 결과에는 영향이 없습니다. (`세아제강_확인요청.md` 에 문항으로 남김)

`job_creator` 의 RB 적격 판정은 외경을 **정확히 일치**로 보는데(`d not in valid_rb_diameters`),
같은 파일의 `get_tool_info` 는 ±5mm 근사를 씁니다. 시뮬레이터는 ±5mm 로 통일했습니다 —
현재 오더셋에서는 차이가 0건이지만(해당 외경 제품이 두께 조건에서 이미 탈락), 다른 계획서에서는
OD 660.4 · 711.2 같은 값이 갈릴 수 있어 역시 확인 문항으로 남겼습니다.

### 검증 보강

`source/tools/verify_formulas.js` 에 **specs.py 원본 대조 고정 기대값 7건**을 추가했습니다
(원본은 사내 자료라 저장소에 포함하지 않으므로 결과값만 고정). 전 항목 PASS.

---

## 정본 확정 — specs.py 기준을 기본값으로 + 「계획 실행」 원클릭 워크플로우

2026-08-06 세아제강 카톡 피드백 3건을 반영했습니다.

### 1. “specs.py 가 맞습니다” — 확관 제약 정본 확정

> - PPT 자료와 운영 모델(ortools) 안의 `specs.py` 가 다르면 **`specs.py` 가 정본**이다.
>   PPT 제작 이후 계속 수정됐고, PPT 에는 생략된 내용도 있다.
> - **12.802m 제품은 #1·#2호기 모두 투입 가능** — 실제 생산 로그에 Expander #2호기 이력이 있다.

지난 버전은 두 자료가 갈리는 4개 항목을 `EXP_RULESET` 토글로 병기하고 **PPT 기준을 기본값**으로 두었습니다.
이제 **운영 모델(specs.py)이 정본**입니다. PPT 기준은 대조용으로만 남깁니다.

| 항목 | 종전 기본값 (PPT) | **새 기본값 (specs.py)** |
|---|---|---|
| #2호기 길이 상한 | 12.8m 이상 불가 | **12.8384m 초과 불가** |
| #1호기 길이 상한 | 14m 이상 불가 | **14.021m 초과 불가** |
| 외경 48"↑ / 22"↓ | #2호기 우선(soft) | **#2호기 전용(hard)** |
| RB 투입 조건 | t ≤ 25T & OD ≤ 24" | **RB 다이표 외경(24~48") & 9 ≤ t ≤ 25.4** |
| 확관 횟수 N | 엑셀 표준시간 분석 (홀수) | **#1 `round(L/(step−150))` · #2 짝수 보정** |

물량 분류가 통째로 바뀝니다 (총 1,446본).

| 구분 (본) | 구버전 기준 | 정본 기준 |
|---|---:|---:|
| #1호기 전용 | 960 | **0** |
| #2호기 전용 | 0 | 62 |
| 두 호기 다 가능 — 배분 규칙 대상 | 111 | **941** |
| #1·#2 동시 가동 | 350 | 350 |
| RB 라인 | 25 | 93 |

시뮬레이션 기준값 (2교대 7.5H · 공구 계층 셋업):

| | 구버전 기본값 | **새 기본값** |
|---|---:|---:|
| Makespan (EAT) | 28.4일 | **32.4일** |
| 확관 #1 / #2 처리 본수 | 1,354 / 417 | **802 / 901** |
| 확관 호기 부하 편차 | 141.0h | **20.6h** |
| 1위 병목 | 포장 및 출하 85% | 포장 및 출하 75% |
| 최적화(OPT) | 26.3일 · 전환 56.5h · 편차 133h | **29.5일 · 전환 47.5h · 편차 8.1h** |

> **진단이 바뀝니다.** 종전 결론이던 “확관 호기 부하 편차는 배분 규칙이 아니라 제품 길이 구성 때문”은
> PPT 기준에서만 성립합니다. 정본 기준에서는 편중이 사라지고, 배분 규칙이 손댈 수 있는 물량이 8배 늘어납니다.
> 대신 N 산출식이 바뀌면서 확관 소요가 늘어 **Makespan 이 +14%** 입니다.

화면 문구도 전부 정본 기준으로 바꿨습니다 — 제약표 「현재 적용 기준」 열, 적격성 카드,
「확관 제약 기준」 셀렉트 라벨, 병목 탭 제약 요약. 종전의 「두 자료가 다릅니다 — 확인 필요」 경고 박스는
**「정본 확정 (2026-08-06)」 안내 + 두 기준 비교표**로 바뀌었습니다.

### 2. 「계획 실행」 탭 — 업로드 → 최적화 → 시뮬레이션 원클릭

> - 최적화 엔진을 한 번 돌리고, 거기서 나온 스케줄대로 시뮬레이션이 흘러가게 할 것
> - 다른 조관계획서도 업로드해서 흘려 볼 수 있게 할 것

기능은 이미 있었지만 「계획서·설정」 → 「확관 최적화」 → 「공정 흐름」 세 탭을 오가야 했습니다.
**첫 화면에 4단계 워크플로우 탭을 신설**했습니다.

- **「▶ 전체 자동 실행」 버튼 하나** — 최적화 엔진 실행 → 배분 규칙을 `OPT` 로 전환 → 그 스케줄대로 전체 공정 시뮬레이션
- 1단계에 조관계획서 업로드 드롭존을 옮겼습니다 (「계획서·설정」 탭 ①은 이 탭으로 연결)
- 상단 4칸 진행 표시 — 계획서 / 제약·조건 / 최적화 해 / 시뮬레이션 결과
- **최적화 전후 비교** — 직전 배분 규칙 대비 Makespan · 확관 전환 · 호기 부하 편차 증감률
  (기본 데이터 기준: EAT 32.4일 → OPT 29.5일 −9% · 전환 75.5h → 47.5h −37% · 편차 20.6h → 8.1h −61%)
- 계획서를 올려도 탭이 튀지 않고 그 자리에서 2·3단계로 이어집니다

### 3. 저장소 구조 정리

산출물 HTML 을 **저장소 맨 위**로 올리고, 나머지를 **`source/` 하나**로 모았습니다.
"실제로 필요한 건 HTML 하나인데 폴더가 너무 많다"는 요청 반영.

```
JCOE_Simulator.html   ← 받아서 열면 끝
JCOE_3D.html
README.md · CHANGELOG.md · 세아제강_확인요청.md
source/  build.py · build3d.py · src/ · src3d/ · data/ · vendor/ · tools/ · testdata/
```

- `source/build.py` → `../JCOE_Simulator.html`, `source/build3d.py` → `../JCOE_3D.html` (출력 경로 변경)
- `verify_ui.js` 가 `process.cwd()` 대신 `__dirname` 기준으로 HTML 을 찾도록 수정
- `.gitignore` 의 `!testdata/*.xlsx` → `!source/testdata/*.xlsx`
- **삭제** — `legacy/`(옛 산출물 HTML 5개 65MB + 실행 불가한 `gen_views.py`),
  `docs/slide4·5.png`(PPT 캡처 — PPT 가 구버전으로 확정되어 오해 소지),
  `.github/workflows/summary.yml`(이 프로젝트와 무관한 GitHub 기본 템플릿)

### 변경 파일

```
source/src/flow.js       EXP_RULESET 순서·라벨 · EXP_RULESET_DEFAULT='ortools'
source/src/engine.js     EXP_NMODE 기본값 'ortools'
source/src/shell.html    pWiz 탭·패널 신설(랜딩 탭) · 위저드 CSS · 셀렉트 기본값/라벨 · pCfg ① 연결
source/src/ui.js         initWizard/renderWiz/snapKpi/goTab · optKpiHtml 분리 · renderRuleDiff 재작성
                         readCfg 기본값 · runSim 에서 renderWiz 호출
source/build*.py         출력 경로를 저장소 루트로
source/tools/verify_formulas.js  N 검증을 두 모드로 분리 (엑셀 21/25 · 운영모델 23/24)
source/tools/verify_ui.js        위저드 원클릭 시나리오 추가 · 경로 기준 변경
README.md                구조 재작성 + 「이 파일 하나만 받으세요」 안내
세아제강_확인요청.md         ①~④ 회신 완료 처리, 남은 항목 재정리 (docs/ 에서 루트로 이동)
```

### 검증

`node source/tools/verify_formulas.js` 전 항목 PASS · `node source/tools/verify_ui.js` 2D/3D 콘솔 오류 0.
구조 변경 후 재빌드·재검증까지 완료했습니다.

---

## 데이터 오류·충돌값 일괄 수정

엔진·모델·화면·계획서 로더를 전수 감사해 확인된 결함을 고쳤습니다.
각 항목은 node / 헤드리스 브라우저로 재현을 확인한 뒤 수정했습니다.

### A. 계산이 틀렸던 것

| 항목 | 증상 | 수정 후 |
|---|---|---|
| **관단 X-ray 가 전장 촬영으로 계산** | `XE` 와 `FX` 가 둘 다 `st:'RT'` 인데 `rtType` 이 `450kV` 하드코딩. 관단부만 찍는 공정이 전장 촬영(1,015초)으로 계산 | 노드에 `rtType` 부여 — `XE`=End-RT(400.6초). **XE 부하 251.4h → 95.8h, 가동률 59.0% → 22.4%.** 병목 6위에서 하위권으로 |
| **주말 비가동 시 가동률 35% 과소** | 가용시간 분모를 `달력일수 × 1일가용` 으로 계산해 주말이 가용시간에 포함됨 | `cal.capBetween(t0,tEnd)` 신설 — 실제 근무 시간만 적산. **주말 비가동 시 확관 가동률 48.8% → 72.8%** |
| **재작업 시 포장이 2회 실행** | 재작업 경로를 `queue.unshift(...)` 로 넣으면서 남아 있던 꼬리(`PACK`)를 비우지 않아 중복 | 꼬리를 비우고 재구성. `pDefect 5%` 기준 **PACK 작업량 +4.4% 과대 계상 제거** |
| **RB 전환시간을 #1호기 다이표로 계산** | RB 노드는 풀 인덱스가 0 이라 `EXP_MACHINES[0]='M1'` 이 넘어감. 가공시간은 RB, 전환시간은 M1 기준 | `n.machine` 우선 사용 — 작업시간과 전환시간이 같은 호기 기준 |
| **다이표에 없는 규격끼리 "교체 없음 0초"** | `toolInfo` 가 매칭 실패 시 모두 `die:'UNKNOWN'` 을 반환해, 서로 완전히 다른 규격 사이에서도 전환시간이 0 | 미매칭 시 규격 자체를 공구 키로 사용. 규격이 다르면 최소 다이 교체(90분)를 보수적으로 부과하고 「공구 미상」 표시 |
| **룩업 표 "구멍"에서 최악값 채택** | `pickRange` 가 구간 사이 틈(71곳)에 떨어진 값에 마지막 행(가장 두꺼운=가장 느린)을 반환. 두께 29.05mm 에서 내면 SAW **+29%**, 10.005mm 에서 1차 U.T **+65%** | 가장 가까운 구간을 쓰고 동점이면 위쪽(보수적) 구간 |
| **운영모델 N 식의 분모 붕괴** | `step−150` 이라 step=151mm 에서 분모가 1 → N 이 12,802 까지 폭발 | 분모 하한 50 |
| **재질 대리변수 경계 불일치** | `specOf` 는 `t>=25` 로 고강도 판정, Gap Press 투입은 `t>25`. t=25.0 제품이 Gap Press 는 안 타면서 재질만 고강도로 잡힘 | 경계를 `t>25` 로 통일 |
| **호기 부하 편차가 3호기를 무시** | `expBalanceH` 가 `units[0]`·`units[1]` 만 비교 | 전 호기 최대−최소. 몬테카를로도 `u3`·`balH` 기록 |
| **SPEC 배분 규칙이 #1호기를 강제** | 외경 48"↑/22"↓ 가 아닌 규격을 `want=0` 으로 #1호기에 강제 배정. 3호기는 어떤 경우에도 선택 불가 | 우선 규격만 #2·#3호기 우선, 나머지는 가장 빨리 비는 호기 |
| **확관 처리 본수 이중 계상** | 동시가동(BOTH)은 두 호기가 같은 파이프를 잡아 `jobs` 가 2회 증가 → 확관 1,771본(총 1,446본) | 노드 「처리 본수」는 실제 파이프 수(1,421), 호기별 합계는 `unitJobs` 로 분리 |

### B. 조용히 잘못 동작하던 것

| 항목 | 증상 | 수정 후 |
|---|---|---|
| **낡은 최적화 해가 라우팅만 바꿈** | `useRBLine` 이 `dispatchRule` 을 보지 않아, EAT 로 돌려도 남아 있는 plan 이 RB 라우팅을 결정. RB 라인이 조용히 비워짐 | plan 은 `OPT`/`IMPORT` 일 때만 참조 |
| **제약을 바꿔도 배분 규칙이 OPT 로 남음** | `pickExpander` 에 `OPT` 분기가 없어 조용히 EAT 로 떨어짐. 화면 세 곳(셀렉트/③ KPI/실제 시뮬)이 서로 다른 값을 말함 | `invalidatePlan()` — 규칙을 EAT 로 되돌리고 ③ 패널에 「재실행 필요」 표시. `cfgRB` 도 무효화 대상에 추가 |
| **반복 실행이 가짜 6번째 규칙을 표시** | 가져온 스케줄이 없어도 `IMPORT` 가 목록에 포함돼 내장 SA 결과를 "외부 최적화 스케줄" 로 한 줄 더 표시 | `ruleKeys()` 로 필터. 비교·반복 실행 모두 적용 |
| **Force_RB 가 조용히 0본** | 계획서에 `병목 공정 작업장`·`원재료 내역` 열이 없으면 판정 근거가 없어 RB 물량 0. 경고 없음 | 근거 열이 없으면 경고 배너 |
| **변동성 위젯이 빈 핸들러** | 「변동성 사용」을 켜도 안내 문구가 갱신되지 않음 | `renderStNote` 연결 |
| **실가동 h/Shift 를 비우면 브라우저 정지** | 빈 값이 0 이 되어 가용시간 0 → 작업마다 4,000회 루프. 간트가 54만 개 div 생성 | `numIn()` 검증(0.5~24). `cfgHold`·`cfgSeqGap` 도 동일 |
| **분모 0 → 화면에 NaN** | 전환시간을 끄면 병목 탭·규칙 비교에 `NaN%` 3곳 | `pct()`·`delta()` 헬퍼로 `—` 표시 |
| **오더번호가 이스케이프 없이 HTML 로** | `<`·`"` 가 든 오더번호를 올리면 In/Out 셀렉트와 간트가 깨짐 | `esc()` 적용 |

### C. 화면 문구가 모델을 따라가지 않던 것

「확관 제약 기준」을 전환해도 고정 문구가 그대로 남아 **운영 모델 기준에서 틀린 설명**을 보여주던 곳을 전부 동적으로 바꿨습니다.

- 적격성 카드 「12.8~14m → #1호기 전용」 → 실제 사유(길이/외경)를 집계해 표시
- 병목 탭 해설의 「1본 소요 포장 873초 … 확관 483초」 → 대표 규격을 실제 산출식으로 재계산
- 병목 탭 해설의 「물량의 66%가 한 대에 몰립니다」 → 현재 기준으로 매번 계산
- 공정 흐름도 캔버스 주석(RB 조건·길이 상한) → 현재 기준 반영, 적용 기준명 표시
- 「확관 최적화」 탭 ① 표·설명 박스, 「병목 분석」 탭 제약표 → 현재 적용 기준 병기
- 확관 3호기 행을 「확관 3호기 (= RB 라인)」 로 표기, RB 노드의 고정 `sub` 라벨 제거

### D. 조관계획서 로더

| 항목 | 증상 | 수정 후 |
|---|---|---|
| **m 단위 계획서를 쓸 수 없음** | 길이 열 숫자 보완이 mm(4,000~25,000)만 인정. 수동 지정해도 `guessUnits` 가 재계산되지 않아 mm 로 굳음 | m 범위(4~25) 추가, 열 변경 시 단위 재추정 |
| **0건일 때 사유가 숨겨짐** | 조기 `return` 으로 「값 범위 이탈」 상세가 표시되지 않음 | 0건이어도 건너뛴 사유 표시 + 단위 힌트 |
| **원재료내역에서 히트번호를 길이로 오인** | `SLAB 914*9.3*12802 HEAT NO.45012` → 45012 채택 → 길이비 3.5 → 배척으로 오판 → RB 강제 투입 | 라벨 뒤 식별번호 제거, `OD*t*L` 패턴 우선, 콤마 처리. 판정 불가면 0 |
| `판매오더` 열이 없으면 오더번호가 `R1,R2…` | CP-SAT 스케줄 매칭 0건 | `오더` 를 낮은 우선순위로 인식 사전에 추가 |
| 수량 0 인 행 사유가 「수량 없음」 | 원인 파악이 어려움 | 「수량 0」 으로 구분 표시 |

### E. 정리

- `EXP_LIMIT` 죽은 코드 제거 (실제 한계값은 `expRules()` 가 결정하는데 값이 어긋난 채 남아 있었음)
- 확관 노드에서 `STD.Expander` 를 잘못된 인자로 한 번 더 호출하던 것 제거
- `DIE_KEY` 에 `M3:'M2'` 명시 (종전에는 우연한 폴백에 의존)
- `kpi.expUtilAvg` / `kpi.expUnitUtil` 추가 — `expUtil` 이 "가장 바쁜 1대" 임을 오해하지 않도록
- `toolInfo` 가 두께 근사 거리(`tDiff`)를 반환. 3mm 이상이면 표준시간 탭에 경고

### 수정 후 기준값 (EAT · 공구 계층 셋업 · 다이어그램 기준)

| | 수정 전 | 수정 후 |
|---|---|---|
| Makespan | 28.4일 | 28.4일 |
| 확관 전환 | 77.0h | 77.0h |
| 병목 1~3위 | 포장 85% · 슬러그 70% · 확관 64% | 동일 |
| **관단 X-ray** | **59.0% (6위)** | **22.4% (하위권)** |
| **주말 비가동 시 확관** | **48.8%** | **72.8%** |

납기(Makespan)는 그대로지만 **설비 진단 결론이 바뀝니다.** 관단 X-ray 는 병목이 아니었고,
주말 비가동 계획에서는 확관이 훨씬 더 타이트합니다.

---

## `04c275d` 확관 적격 규칙 2종 기준 병기 + RB 1근 캘린더 + CP-SAT 스케줄 가져오기

**출처** — 확관 공정 최적화 운영 모델 `ortools_final_v3/ortools_final_v2_ep12`
(`specs.py` · `job_creator.py` · `optimizer_grouped.py` · `data_loader.py`)

### 1. 확관 적격 규칙 — 두 자료가 어긋나 **기준 자체를 전환 가능하게** 했습니다

공정 다이어그램·제약표(슬라이드 3·4)와 운영 모델이 네 항목에서 갈립니다. 어느 쪽이 맞는지
단정할 근거가 없어 `EXP_RULESET` 으로 양쪽을 모두 넣고 화면에서 전환하게 했습니다.

| 항목 | `ppt` (다이어그램·제약표) | `ortools` (운영 모델) | 근거 |
|---|---|---|---|
| #2호기 길이 상한 | 12.8m 이상 불가 | 12.8384m **초과** 불가 | `optimizer_grouped.py:85` `elif length > 12.8384` |
| #1호기 길이 상한 | 14m 이상 불가 | 14.021m **초과** 불가 | `specs.BOTH_LENGTH_THRESHOLD = 14.021` |
| 외경 48"↑ / 22"↓ | #2호기 **우선**(soft) | #2호기 **전용**(hard) | `optimizer_grouped.py:88` `m2_jobs` 분기 |
| RB 투입 조건 | t ≤ 25T & OD ≤ 24" | RB 다이표 외경(24"~48") & 9 ≤ t ≤ 25.4 & L ≤ 12.8 | `job_creator.py:455-461` |

**영향이 가장 큰 항목은 #2호기 길이 상한입니다.** 현재 오더셋 물량의 66%가 12.802m 인데,
12.8m 기준에서는 #2호기 불가(#1호기 전용), 12.8384m 기준에서는 두 호기 다 가능이 됩니다.

| 구분 (본, 총 1,446) | `ppt` | `ortools` |
|---|---:|---:|
| #1호기 전용 | **960** | 0 |
| #2호기 전용 | 0 | 62 |
| 두 호기 다 가능 (배분 규칙 대상) | 111 | **941** |
| #1·#2 동시 가동 | 350 | 350 |
| RB 라인 | 25 | 93 |

시뮬레이션 결과 (EAT 규칙, 공구 계층 셋업 기준):

| 기준 | Makespan | 확관 전환(h) | #1호기 | #2호기 | 1위 병목 |
|---|---:|---:|---:|---:|---|
| `ppt` | 28.4일 | 77.0 | **64%** | 27% | 포장 85% |
| `ortools` | 28.5일 | 75.5 | 41% | 43% | 포장 85% |

최적화(OPT) 적용 시 격차가 더 벌어집니다 — `ppt` 27.4일 / 확관 전환 56.5h,
`ortools` **26.4일 / 확관 전환 43.5h**. 배분 규칙이 손댈 수 있는 물량이 8배 늘기 때문입니다.

> 다이어그램 기준에서 도출했던 결론 — "호기 부하 편차는 배분 규칙이 아니라 제품 길이 구성에서
> 나온다" — 는 `ppt` 기준에서만 성립합니다. `ortools` 기준에서는 편중 자체가 사라집니다.

### 2. Force_RB 신설 → **확관 3호기 = RB 라인** 확정

운영 모델의 RB 강제 투입 조건이 제약표의 확관 3호기 설명과 정확히 대응합니다.

| 제약표 「확관 3호기」 | 운영 모델 `Force_RB` |
|---|---|
| 열처리 공정 제품 우선 투입 | 병목공정 == `HT102` |
| 배척 제품 우선 투입 | 원재료길이 / 제품길이 ≥ 1.8 (원판 1장 → 2본) |

그동안 "확관 3호기가 실재하는가 / 다이어그램의 2라인과 모순 아닌가"를 미확인으로 남겼는데,
별개 설비가 아니라 RB 라인이었습니다. `useM3` 토글은 "3호기를 별도 설비로 취급" 옵션으로만 남깁니다.

RB 투입 모드를 3종으로 선택할 수 있습니다 — 적격 제품 전량 / Force_RB 만 / RB 미사용.
실 계획서 기준 Force_RB 해당 물량은 37본(HT102 + 배척)입니다.

### 3. RB 라인 1근무조 — 설비별 가용 캘린더 분리

운영 모델은 RB 소요시간에 `rb_multiplier = shift_m1m2` 를 곱해 보정합니다
(RB 1근 vs M1/M2 2~3근). 시뮬레이터는 소요시간을 건드리지 않고 **캘린더 자체를 분리**했습니다.

- `makeCalendar(cfg, shiftsOverride)` — RB 노드는 `cfg.rbShifts`(기본 1)로 별도 생성
- 가동률도 설비별 가용시간 기준 — RB 1근 16% ↔ 2근 8% (같은 작업량, 분모가 다름)
- 종전에는 전 설비 동일 캘린더라 RB 부하를 절반으로 과소평가했습니다

### 4. 외부 최적화 스케줄 가져오기 (CP-SAT → 시뮬레이터)

「확관 최적화」 탭 ④. `solve_integrated_schedule()` 출력 DataFrame 을 CSV/XLSX/JSON 으로 올리면
**호기 배정과 투입 순서를 그대로** 흘립니다. 배분 규칙 목록에 `IMPORT` 가 추가되어
내장 SA 최적화와 나란히 비교됩니다.

- 필수 열 — `OrderNo`(판매오더) · `Machine`(`Expander#1` / `Expander#2` / `Expander#RB` / `Both`)
- 선택 열 — `Ex_Start`(투입 순서 재현) · `Ex_End` · `Quantity` · `Length` · `Diameter` · `Thickness`
- 한글 헤더(`판매오더` / `설비` / `호기` / `시작`)도 인식, 헤더 행 위치 자동 탐색
- 같은 판매오더가 Ready/Unready 두 행이면 앞 행 배정 채택 (운영 모델의 동일설비 강제 제약과 동일)
- `Machine` 이 `Expander#RB` 인 오더는 라우팅 단계에서 RB 라인으로 보냅니다

### 5. 조관계획서 로더 — 실제 스키마 대응

`data_loader.load_orders` 가 읽는 실제 열 이름에 맞춰 인식 사전을 보강했습니다.

- 시트 `JCOE` / `일  자`(공백 2칸) · `오더` · `판매오더` · `원재료코드` · `계획수량` ·
  `외경` · `두께` · `길이`(**m 단위**) · `긴급` · `병목 공정 작업장` · `원재료내역`
- `병목 공정 작업장` → `bottleneck` (HT102 판정)
- `원재료내역` → 원판 길이 추출 → `rawL` (배척 판정). `SLAB 762*9.3*25604` 같은 문자열에서
  3,000~60,000mm 범위 값을 뽑습니다
- 열 지정 UI에 두 항목(선택)을 추가 — 자동 인식이 틀리면 직접 지정 가능
- `testdata/plan_jcoe_real.xlsx` — 실제 스키마를 그대로 흉내낸 검증용 계획서 추가

### 변경 파일

```
src/flow.js        EXP_RULESET · expanderMode · rbCapable/forceRB/useRBLine · importOptPlan
                   makeCalendar(shiftsOverride) · simulate 의 RB 캘린더 분기 · 가동률 분모 분리
src/ui.js          readCfg 확장 · renderRuleDiff · initImportTab/readImport/renderImport
src/shell.html     확관 제약 기준·RB 투입·RB 근무조 select, ④ 가져오기 섹션
src/planload.js    KEYS.bn / KEYS.rawL · extractRawLength · 열 지정 UI 2칸 추가
tools/runsim.js    노출 API 확장
testdata/          plan_jcoe_real.xlsx
```

---

## `6307a1c` 확관 셋업 공구 계층 룰 + 호기별 다이 스펙 + N 산출식 병기

**출처** — 확관 공정 최적화 운영 모델 `specs.py`
(`get_tool_info` · `get_setup_time_val` · `die_specs_m1/m2/rb` · `calculate_time_m1/m2/rb`)

### 1. 확관 전환시간 — 치수 변화폭 추정치 → 공구 계층 룰

종전에는 직전 제품과 외경/두께/길이 변화폭에 따라 제가 추정한 값을 더했습니다.
실제는 **장착 공구가 바뀌는지**로 결정되며, 상위 공구부터 판정합니다.

| 바뀌는 공구 | 셋업 시간 | 판정 |
|---|---:|---|
| Drawbar | **270분** | 헤드그룹 450 → `SMALL`, 그 외 → `LARGE` (RB 는 `RB_{head}`) |
| Head Group | **150분** | 450 / 600 / 800 / 1000 |
| Die | **90분** | 외경 + 두께구간 조합 |
| 동일 | 0 | |

공구는 `toolInfo(od, t, machine)` 이 호기별 다이 스펙표에서 **외경 ±5mm 근사 + 두께 최근접**으로
찾습니다 (`specs.get_tool_info` 와 동일 로직).

효과 — 확관 전환시간이 20~25% 줄고, 1위 병목 순위는 유지되지만 확관의 성격이 뚜렷해집니다.

| 배분 규칙 | 확관 전환(h) 치수 변화폭 | 공구 계층 | 차이 |
|---|---:|---:|---:|
| EAT | 96.0 | **77.0** | −20% |
| SETUP | 70.5 | **59.0** | −16% |
| OPT | 59.5 | **56.5** | −5% |

확관은 **전환 비중 20.7%로 전 설비 중 압도적 1위**입니다(나머지는 대부분 2% 미만).
"확관은 느려서 병목이 아니라 갈아끼우느라 병목"이라는 진단이 실측 근거로 확인됩니다.

### 2. 다이 스펙표를 호기별로 분리

종전에는 엑셀 「Expander(1호기)」 표 하나를 M2·RB 에도 공용으로 적용했습니다
(엑셀 「Expander(2호기)」 시트가 비어 있었기 때문). 운영 모델에 3종이 모두 있습니다.

| 표 | 행 수 | 예: OD914 t9.3 |
|---|---:|---|
| `die_specs_m1` | 107 | `9t(700mm)` |
| `die_specs_m2` | 68 | `9.5t(580mm)` |
| `die_specs_rb` | 37 | `9t(700mm)` |

같은 제품이라도 호기별 step size 가 달라 확관 횟수 N 과 소요시간이 달라집니다
(위 예에서 #1호기 N=21 / #2호기 N=25). `data/tables.json` 의 `dieSpec` 키에 들어 있습니다.
M2 표의 OD 1473·1574 는 빈 리스트 — 해당 외경 취급 불가로 보입니다.

### 3. 확관 횟수 N — 산출식 병기 + 토글

두 자료의 N 계산이 다릅니다. **어느 쪽이 맞는지 확인 필요**하여 양쪽을 넣고
「계획서·설정」 탭에서 전환하게 했습니다.

| | #1호기 | #2호기 |
|---|---|---|
| 「JCOE 공정 생산 표준 시간 분석」 | `N = ceil((L−500)/step) + 2`, 짝수면 +1 → **항상 홀수** | 동일 |
| 운영 모델 `specs.py` | `N = round(L / (step − (step≤150 ? 100 : 150)))` | `ceil((L−500)/step)+2`, 홀수면 +1 → **항상 짝수** |

엑셀 주석은 "N의 값은 항상 홀수", 운영 모델 `calculate_time_m2` 는 `if n_val % 2: n_val += 1`
(홀수면 +1 → 짝수)로 **정반대**입니다.

step 이 작을수록(=두꺼운 관) #1호기 격차가 급격히 벌어집니다.

| L(mm) | step(mm) | N(엑셀) | N(운영모델) | #1호기 시간 차 |
|---:|---:|---:|---:|---:|
| 12,802 | 700 | 21 | 23 | +5% |
| 12,802 | 460 | 29 | 41 | +25% |
| 12,802 | 300 | 45 | 85 | +62% |
| 12,000 | 110 | 107 | 1,200 | +867% |

전체 시뮬레이션에 미치는 영향도 작지 않습니다.

| N 산출식 | Makespan | 확관 전환(h) | #1호기 | 1위 병목 |
|---|---:|---:|---:|---|
| 엑셀 표준시간 분석 | 28.4일 | 77.0 | 64% | 포장 85% |
| 운영 최적화 모델 | **32.3일** | 78.5 | **75%** | 포장 75% / **확관 75%** |

운영 모델 식을 쓰면 소요가 14% 늘고 **확관이 포장과 함께 공동 1위 병목**으로 올라옵니다.

### 4. 검증 보강

`tools/verify_formulas.js` 에 항목 추가 — 공구 판정(헤드/step), 셋업 4단계(0 / 90 / 150 / 270분),
호기별 다이 step, N 산출식 비교. 전 항목 PASS.

### 변경 파일

```
src/engine.js            toolInfo · expanderStep(machine) · expanderN(machine) · EXP_SETUP
                         expanderSetup · changeoverSec(…, machine) · setExpSetupMode/setExpanderNMode
src/flow.js              changeoverSec 호출부에 machine 전달 (5곳)
src/ui.js                readCfg 에서 엔진 전역 모드 반영, 표준시간 탭 호기별 N/step/공구 표시
src/shell.html           확관 셋업 산출 · 확관 N 산출식 select
data/tables.json         dieSpec { M1: 107행, M2: 68행, RB: 37행 } 추가
tools/verify_formulas.js 공구·셋업·N 검증 추가
```

---

## `18cdaac` 가동률 정의 수정 + 병목 유형 분류 추가

- 병렬 설비 가동률을 **대수로 나눈 평균 → 설비 1대 기준 최댓값**으로 변경.
  확관 2대 평균 44%였지만 #1호기만 보면 63% — 호기 편중이 평균에 가려져 있었습니다.
- `utilAvg` / `unitUtil` / `imbalance`(최대−최소) / `setupShare`(전환 비중) 지표 추가
- 병목 3분류 — **가공 병목**(포장·슬러그 제거) / **전환 병목**(확관) / **제약 병목**(호기 편중)

## `71ad2a0` 계획 기간 지정 + 3D 레이아웃 재설계

- 시작일·마감일·오더 투입일(계획서 날짜 / 교대 간격 / 순차) 지정, 기간 내 달성률 표시
- 3D 를 5열 그리드로 재배치 — 설비 간 간격을 벌리고 각 설비 앞 대기 야드를 분리 표시

## `bc08ed9` 확률 변동 시뮬레이션 + 반복 실행 + 재생 컨트롤

- 작업시간·전환시간 로그정규 변동, 불량 라우팅(Repair → 보수용접 / 재확관), MTBF/MTTR
- 몬테카를로 N회 반복 → 완료일·전환시간·재작업 P10/P50/P90 히스토그램
- 타임라인 슬라이더로 임의 시점 점프, 시드 고정 재현

## `4631fd0` 조관계획서 엑셀 업로드 기능

- xlsx/xlsm/xls/csv 드래그 앤 드롭, 헤더 행·열 자동 인식, 단위(mm/inch, mm/m) 추정

## `3af93d6` 초기 커밋

- 엑셀 「Total Summary」 19개 공정 표준시간 산출식 엔진, 공정 흐름 애니메이션,
  확관 배분 규칙 5종, SA 최적화 엔진, 3D 공정 뷰
