# 세아제강 확인 요청

POSTECH IDEA Lab · JCOE 공정 시뮬레이터
최종 갱신 **2026-08-14**

2026-08-14 회신 5건을 전부 반영했습니다. **판단으로 닫을 수 있는 건 닫았고**,
저희가 만들 수 없는 정보만 아래 3건으로 남겼습니다.

---

# 확인 요청 — **없습니다** (2026-08-14 회신으로 전건 종결)

2026-08-14 회신 두 건으로 남아 있던 항목이 모두 닫혔습니다.

## ✅ 「나머지는 전에 드렸던 20251218 엑셀 참고하시면 됩니다」 → 이미 반영돼 있었습니다

확인해 보니 **20251218 엑셀에서 읽을 수 있는 표는 전부 이미 들어와 있습니다.**

| 표 | 출처 | 상태 |
|---|---|---|
| 확관 다이표 #1호기 107행 | 20251218 `Expander(1호기)` 시트 | ✅ 추출 완료 (`tables.json.expanderDie`), specs.py 와 **0건 차이** |
| 확관 다이표 R/B 37행 | 20251218 `Expander(RB)` 시트 | ✅ 추출 완료, 전 행 일치 |
| 확관 다이표 #2호기 68행 | `Expander(2호기)` 시트가 **이미지** | ⚠ 엑셀에서는 읽을 수 없음 → `specs.py` 정본 사용 |
| 표준시간 산출식 20개 | `Total Summary` 시트 | ✅ 전 항목 대조 완료 (골격·상수 전부 일치) |
| #1·#2호기 N 산출식 | `Total Summary` S22·S23 **이미지** | ✅ 이미지 추출로 확보 (`data/evidence/`) |

**#2호기 68행만 엑셀 쪽 원본을 못 봤는데**, 그건 시트가 이미지라 파일을 다시 받아도 마찬가지입니다.
그리고 2026-08-06 에 「**specs.py 가 맞습니다**」로 확정해 주셨으므로 그대로 씁니다.
→ **더 이상 요청드릴 것이 없습니다.**

## ✅ 「모관검사는 공정시간 식이 없으면 공정흐름에 추가 안 하셔도 됩니다」

표준시간 엑셀 `Total Summary` 의 20개 공정을 다시 확인했습니다 — **모관검사 산출식이 없습니다.**

```
Edge Miller · Pre Bender · Press Bender · Gap Press · Tack-Welder · Inside-Welder ·
Outside-Welder · 1st-UT · Expander · End-Facing · Outer bead removal ·
Hydraulic Tester · Final-UT · RT · 포장 · R/B - Expander     ← 모관검사 없음
```

→ **공정 흐름·시뮬레이션에 넣지 않습니다.** 다만 실적 로그에 379본이 있으므로
「실적 검증」 탭에는 **「모관검사 (산출식 없음 — 공정흐름 미포함)」** 로 표시만 남깁니다.

## ✅ 「R/B 라인은 거의 사용을 안 하는 걸로 알고 있습니다」

실적 로그와 정확히 일치합니다 — 2026-07-16~22 로그에 **EP102 실적이 0건**입니다
(`FC112` · `RT104` · `PK112` 도 0건). 시뮬레이터 기본값도 이미 그에 맞습니다.

| R/B 라인 투입 모드 | 기본 오더셋 58건 결과 |
|---|---|
| **열처리·배척 제품만 (기본값)** | R/B **0본** — 계획서에 열처리·배척 표기가 없어 아무것도 안 감 |
| R/B 미사용 | R/B 0본 (동일) |
| 적격 제품 전량 | R/B 899본 · Makespan 34.0일 · 1위 병목이 R/B RT 98.5% 로 바뀜 |

세 번째 모드는 **현장 재현이 아니라 「R/B 를 전면 가동하면?」 이라는 가정 시나리오**이므로
선택하면 화면에 그 사실을 경고로 띄우도록 했고, 선택지 순서도 뒤로 내렸습니다.

---

# 정정 — 「포장 표준시간이 65% 느리다」는 저희 측정 오류였습니다

2026-08-14 오전에 「포장 실적 530s vs 표준 873s (0.61) → 산출식 재확인 필요」라고 보고드렸는데,
**틀렸습니다.** 산출식은 맞고, 저희가 실적을 잘못 재고 있었습니다.

## 무엇이 틀렸나

실적 로그는 **스냅샷 poller** 라 한 간격이 반드시 1본이 아닙니다 — 누적이 2~4씩 뛰는 구간이 있습니다.
그건 `Δt / Δ누적` 으로 나눠 처리했는데, 문제는 그 다음이었습니다.
그렇게 나온 본당 값들의 **중위수**를 표준시간과 비교했습니다.

완료 간격 분포는 **돌 때는 짧고 멈추면 아주 긴** 극단적인 오른쪽 꼬리 형태입니다.
중위수는 그 꼬리를 통째로 버리기 때문에 **가동 중 페이스만** 집어냅니다.
표준시간은 대기를 포함한 평균 사이클과 비교해야 하는데, 사과와 오렌지를 비교한 셈입니다.

| 포장 PK113 추정치 | 값 |
|---|---:|
| 중위수 (종전에 쓴 값) | 461 s |
| 평균 | 918 s |
| **처리량 기준 ΣΔt ÷ ΣΔ누적** | **894 s** |

## 다시 재니 결과가 뒤집힙니다

처리량 기준으로 전 설비를 다시 재면 **모든 공정에서 실적 ≥ 표준** 입니다.
대기·전환·정지가 포함되므로 물리적으로 당연한 모습입니다.
종전 중위수 기준에서 「표준보다 빠르다」고 나왔던 세 공정이 전부 사라졌습니다.

| 설비 | 실적 본당 | 표준식 | 비율 | 여유율 |
|---|---:|---:|---:|---:|
| **PK113 JCOE 포장** | 1,044 s | 873 s | **1.20** | **17%** ← 전 설비 중 최저 |
| FC110 면취 | 967 s | 563 s | 1.72 | 42% |
| HY106 수압 | 932 s | 542 s | 1.72 | 42% |
| EM102 18M Edge Miller | 858 s | 471 s | 1.82 | 45% |
| PM116 18M Press Bender | 919 s | 468 s | 1.97 | 49% |
| EP103 확관 1호기 | 1,399 s | 554 s | 2.52 | 60% |
| EP104 확관 2호기 | 1,370 s | 362 s | 3.78 | 74% |
| UT110 2차 U.T | 900 s | 259 s | 3.47 | 71% |

**여유율이 낮을수록 쉬지 않고 돌고 있다는 뜻**이고, 포장이 17% 로 전 설비 중 가장 낮습니다.
즉 이번 정정은 종전 결론을 뒤집는 게 아니라 **강화합니다** —
「포장이 병목」이라는 §2-3 결론은 실적으로도 그대로 확인됐습니다.

## 시뮬레이터에 반영한 것

- 「실적 검증」 탭의 기본 지표를 **처리량 기준 본당**으로 바꿨습니다. 중위수는 「페이스」 열로 참고만 합니다.
- **여유율** 열을 추가했습니다. 병목 판정은 이 열로 보십시오.
- 「실적 보정」 스위치는 남겨 뒀지만 **보정 대상이 하나도 없습니다** (전 공정 실적 ≥ 표준).
  화면에 그렇게 표시됩니다.

죄송합니다. 확인 요청을 드리기 전에 추정치를 검증했어야 했습니다.

---

# 저희가 판단으로 닫은 것 — 회신 불필요

## ✅ OD1219 `44t(250mm)` / `44t(700mm)` → **250mm 로 확정**

> "인치로 변경하면 인치에 맞는 헤드가 나오게 될 것으로 예상되는데 (250mm)와 (700mm)가
>  어떻게 중복이 되는지 모르겠습니다."

**추출 오류가 아니라 F/L(Full Length)이 다른 실물 다이 2개**이고, **700mm 쪽이 「대체 작업」 다이**입니다.
근거 두 가지입니다.

**(1) 같은 엑셀 RB 시트의 세아제강 셀 메모가 괄호 숫자를 F/L 로 쓰고 있습니다.**

```
SeAH: 32"x34 die (F/L 700mm)  대체 작업 진행중
SeAH: 40"x34die (F/L 700mm)   대체 작업중 / 38"x9 die 실제 없음
SeAH: 다이 40" 44t 사용할 것 · FL 300mm
```

**(2) 그 메모에 나오는 규격이 다이표의 700mm 이상치와 정확히 겹칩니다.**
두께 34t 이상에서 step > 400mm 인 행은 표 전체에 **딱 5개**뿐입니다.

| 행 | 메모 |
|---|---|
| 32" t34 → 700mm | ← 「32"x34 die (F/L 700mm) 대체 작업 진행중」 |
| 40" t34 → 700mm | ← 「40"x34die (F/L 700mm) 대체 작업중」 |
| 44" t44 → 700mm | |
| **48" t44 → 700mm** | ← **문의 대상** |
| 60" t44 → 700mm | |

44t 계열 15행 중 나머지 13행은 전부 **110~300mm** 입니다
(32"→300, 38"→170, 40"→300, 46"→130, 50"→125, 52"→250, 54"→120, 56"→115, 58"→115, 62"→110, 64"→110).
**정규 다이는 250mm 계열이고 700mm 는 대체 다이**로 읽는 것이 표 전체와 유일하게 정합적입니다.

→ **현행대로 250mm 를 씁니다.** 700mm 는 화면에 「대체 다이」로 병기합니다.
혹시 실제로 48"×44t 에 F/L 700mm 를 상시 장착하고 계시면 그때만 알려주십시오
(확관 소요가 1,767s → 507s 로 3.5배 달라집니다).

**헤드는 인치로 정상 매핑되고 있습니다** — OD1219 = 48" → #1호기 헤드 **#1000**, R/B 헤드 **#800**.
호기마다 헤드 구성이 다릅니다(R/B 시트에서 48" 는 #800 그룹).

## ✅ R/B 「옥외 열처리면 15초 제외」 → **문자 그대로 234초 고정**

엑셀 No.20 비고 두 문장입니다.

> ※ 옥외 열처리 제품인 경우, 15초 제외
> → 옥외 열처리 제품이 아닌 경우, 상수 234초 고정

```
옥외 열처리 제품 :  234s 고정            (「15초 제외」 = (N−2)×15 항을 뺀다)
그 외            :  234s + (N−2)×15
```

확관 후 열처리로 치수가 다시 변하므로 R/B 에서는 정형만 하고 확관 횟수 항이 빠진다는 해석입니다.
(초안에서 「기본 상수 234−15 = 219」로 읽었으나, 219 라는 수가 엑셀 어디에도 없어
지어낸 값이 되므로 문자 그대로의 해석으로 되돌렸습니다.)
다르면 알려주십시오 — `cfg.rbHeatRule='none'` 으로 전 제품에 (N−2)×15 를 적용합니다.

## ✅ #2호기 확관 횟수 홀수/짝수 → **specs.py(짝수) 유지**

표준시간 엑셀 `S23` 이미지 + 셀 메모는 「N 은 항상 **홀수**」, 운영 모델 `specs.py` 는 「항상 **짝수**」로
정반대입니다. 그런데 2026-08-06 에 이미 **"specs.py 가 맞습니다"** 회신을 받았으므로
**정본을 그대로 씁니다** (OD914 t9.3 12.802m → N=24). 설정 탭에서 엑셀식으로 전환해 볼 수 있습니다.

한편 **#1호기는 두 자료가 사실상 같은 식**임이 이번에 확인됐습니다 —
엑셀 `ROUNDUP(L/(다이−150))`, 정본 `round(L/(다이−150))`. 분모가 같고 올림/사사오입만 다릅니다.

---

# 이번에 반영한 것 — 2026-08-14 회신 반영

## ① 2호기 시트 원인 규명

`tools/extract_images.py` 신설. 시트별 [값 있는 셀 / 이미지 / 메모] 를 세고 이미지를
`<시트명>__<앵커셀>__<파일명>.png` 로 꺼냅니다. 앞으로 "비어 있다"고 보고하기 전에
반드시 이 스크립트를 먼저 돌립니다. 꺼낸 3장은 `source/data/evidence/` 에 보관했습니다.

## ② OD1219 중복 — 250mm 확정 (위 「판단으로 닫은 것」 참고)

같은 외경·두께에 다이가 둘 이상이면 나머지를 `alts` 로 남기고, 두꺼운 관(t≥34)에서 크게 벌어진
F/L 은 `substitute: true` 로 표시해 화면에 「대체 다이」로 병기합니다.

## ③ R/B 라인 = 확관 다른 호기 — 스펙 전면 반영

보내주신 20251231 엑셀의 `Expander(RB)` 시트와 No.20 산출식으로 아래를 고쳤습니다.

| 항목 | 종전 | 반영 |
|---|---|---|
| **확관 Step Size** | 다이 Size 를 그대로 나눔 | **다이 Size − 90mm** (J4 이미지) |
| 확관 횟수 N | `ceil(L / 다이)` | `ceil(L / (다이 − 90))` |
| 소요 시간 | `234 + (N−2)×15` | 동일 (N 이 바뀜) |
| **수압 · 2차 U.T** | R/B 는 건너뜀 | **본류 HY106 · UT110 을 거침** ← 이번 회신 |
| 가동 조건 | 다이표 외경 & 9≤t≤25.4 & L≤12.8m | 좌동 + **46" 제외** 명시 |
| 헤드 | RB 다이표 그대로 | 좌동 (24~28" → #600, 30~48" → #800) |
| 근무조 | 1근 고정 | 좌동 (엑셀 「7.5H, 1근만 가동」과 일치) |

**Step Size 수정 효과** — OD914 t9.3 12.802m 기준 확관 횟수 19 → **21회**, 소요 489s → **519s**.
종전에는 R/B 확관을 6% 정도 빠르게 잡고 있었습니다.

**R/B 후처리 경로** — `R/B Expander(EP102) → R/B 면취(FC112) → 수압(HY106) → 2차 U.T(UT110)
→ R/B RT(RT104) → 배척 포장(PK112)`.
「확관 최적화」 탭 **R/B 후처리** 에서 구 동작(전용 후처리만)으로 되돌릴 수 있습니다.

`C2` 강관기호 → 옥외 열처리 판정도 넣었습니다(엑셀 No.20 비고). 실적 로그 자재내역에서
`Z483C2X70M` · `Z448C2X65M` · `1C65C22-2` 등 **자재 19종 중 7종 · 작업지시 20건 중 8건**이 C2 였습니다.

## ④ 납기 지연 분석 제거 — 계획서 순서대로만 흘려보내기

- **오더 투입일 기본값을 「계획서 시트 순서대로」로 변경**했습니다. 조관계획서 JCOE 시트의
  **행 순서 그대로** 시작일부터 투입하고, 날짜·납기를 일절 쓰지 않습니다.
- **납기 지연 분석은 기본 해제**입니다(설정에서 켤 수 있습니다). `영업계획납기일` 관련
  KPI·간트 표시가 전부 사라집니다.
- JCOE 시트만 읽는 동작(물종 열이 ERW 인 행 제외)은 종전과 같습니다.

## ⑤ 실적 로그 기반 검증 — 「실적 검증」 탭 신설

`machine_prod_log` CSV 를 올리면 —

1. **설비별 실적** — 본수 · 실적 구간 · **실적 본당 소요(완료 간격 중위수)**
2. **표준시간 검증** — 같은 규격으로 산출식을 다시 계산해 실적과 비율 대조 (위 ④ 표)
3. **실적 오더셋** — WO 별로 자재내역에서 외경·두께·길이를 뽑아 오더셋을 만들고,
   버튼 하나로 그대로 시뮬레이션 (투입 순서 = 최초 완료 시각 순)
4. **실적 보정** — 실적이 표준보다 빠른 공정만 `min(1, 실적/표준)` 으로 하향 보정해 재시뮬레이션.
   기본 오더셋에서 1위 병목이 **포장 96.9% → 확관 80.4%** 로 바뀝니다 (위 「답이 필요한 것」 1번)

`tools/verify_prodlog.js` 로 CLI 에서도 돌아갑니다.

---

# 전수 감사 — 2026-08-14, 계산 오류 10건 정정

시뮬레이터 전 파일을 엑셀 원문과 대조해 다시 훑었습니다. **표준시간 산출식 20개의 골격은
전부 엑셀과 일치**했고(상수 348/283/2163/1810/464/634/234 … 전부 일치, RB 다이표 37행 전 행 일치),
결함은 **조건 분기 · 빠진 항 · 룩업 경계**에 있었습니다. 전부 재현해 확인한 뒤 고쳤습니다.
회귀 검사는 `node source/tools/verify_audit.js` (34항목)로 고정했습니다.

## 계산이 틀렸던 것

| # | 증상 | 원인 | 정정 |
|---|---|---|---|
| **A1** | **Gap Press 소요가 실제의 2~4배** | 엑셀 `464 − L/0.3 + [{ceil(L/6)×65}×2] × 2` 의 **대괄호 밖 ×2 가 「X70 이상」 조건**인데, 코드가 무조건 곱한 뒤 X70 에 또 ×2. 게다가 Gap Press 투입 조건(t>25)과 고강도 대리변수 기준(t>25)이 같아 **투입 제품이 전부 X70 판정** → 늘 4배 | 조건부로 정정. OD1219 t31.2 12.802m **1,981s → 1,201s** (본당 −780s, 기본 오더셋 148본에서 −32h) |
| **A2** | Edge Miller 「첫 본 생산 시 +1분 50초」 누락 | 비고 항이 코드에 아예 없었음 | 오더 첫 본에 +110s. 18M 12.802m **527s → 637s**(첫 본) |
| **A3** | 수압에서 **35" 제품이 36" 상수로 계산** | `round(inch/2)×2` 로 짝수 스냅한 값으로 「36" 이상」을 판정 → OD889(정확히 35")가 36 으로 올라가 2차 압빼기·에어벤트가 180/120 → 300/180 | 공칭 인치로 판정. OD889 **745s → 565s** |
| **A4** | R/B 열처리 기본 상수 219s | 「15초 제외」를 상수에서 빼는 것으로 읽어 **엑셀에 없는 219 를 지어냄** | 문자 그대로 — 열처리 제품은 `(N−2)×15` 항 제외 → **234s 고정** |
| **B1** | **공장 주력 12.802m 제품이 전부 R/B 부적격** | R/B 길이 상한을 `12.8` 로 하드코딩. #2호기는 2026-08-06 회신으로 12.8384 로 확정됐는데 R/B 만 12.8 → 12.802 > 12.8 로 탈락. 1,446본 중 **960본(66%)** 이 해당 | 같은 확관기이므로 `L2 = 12.8384` 적용. 열처리·배척 제품의 R/B 강제가 실제로 동작 |
| **B2** | **가동률이 100% 를 넘는 경우** | 같은 판매오더가 여러 행(R/U 분할)으로 오면 `orderSpan[o.no]` 를 **덮어써서** tEnd·처리량·가용시간이 줄어듦 | 덮어쓰지 않고 합산. 201본 케이스에서 처리량 0.33/일 → 정상 |
| **B3** | 「같은 오더 동일 호기 강제」가 정작 R/U 분할에서 깨짐 | 계획서 로더는 두 번째 행을 `<오더>-2` 로 저장하는데, 스케줄 조회는 정확 일치만 | 접미사 폴백(`planLookup`). 두 행이 같은 호기로 핀됨 |
| **B4** | 설비 코드 → 호기 매핑이 전부 어긋남 | 숫자 부분일치라 **EP102→M2, EP103→M3, EP104→M1** | 명시 표. EP102→RB · EP103→M1 · EP104→M2 |
| **B5** | 포장 「1/10본마다 추가 검사」가 오더 쪼갠 방식에 따라 달라짐 | 전역 순번(`seqGlobal`)을 계산해 두고 오더 내 순번을 넘김. qty<10 오더는 아예 0회 | 전역 순번 전달. 1오더×100본 = 100오더×1본 |
| **B6** | 외부 스케줄이 R/B 를 지정하면 **적격성 무시** | `if (a === 'RB') return true` | 적격성 확인 후 미달이면 본류로 보내고 경고. 18m·t30·46" 제품이 12.8m 설비에서 처리되던 문제 해소 |

## 조용한 실패를 드러낸 것

| # | 종전 | 정정 |
|---|---|---|
| B7 | 재작업 루프가 라우트 상한(300)을 넘으면 그 본이 **말없이 사라짐** | `kpi.routeAborted` 로 집계하고 화면에 「⚠ 미완주 N본」 표시 |
| B8 | 「계획서 시트 순서」 모드에서 납기를 무조건 지워, 납기 분석을 켜도 **항상 "0건 지연"** | 분석을 켠 경우에만 납기 유지 |
| B9 | 실적 보정이 시뮬에만 걸려 **최적화 Cmax 와 축이 어긋남** | 최적화 엔진도 같은 보정 사용 |
| B10 | 자재기호 `C2`(옥외 열처리) 제품이 R/B 로 강제되지 않음 (HT102 열만 확인) | `spec.heat` 도 판정에 포함 |
| B11 | R/B 가 2차 U.T 를 **무조건** 통과 → 본류(API 5L 조건)보다 검사를 더 받음 | 본류와 같은 D5 조건. 수압은 본류처럼 무조건 |
| B12 | 빈 오더셋·qty 0 에서 `makespanH = -Infinity` | 유한값 방어 |

## 감사 결과 이상 없음으로 확인된 것

- **표준시간 산출식 20개 골격 · 상수 전부 엑셀과 일치** (RB 다이표 37행도 전 행 일치)
- 단위(m vs mm) — Gap Press·Press Bender 만 m, 나머지 전부 mm 로 올바름
- 룩업 경계 — `pickRange` 의 71개 구간 틈이 전부 인접 구간으로 해소되고, 표 밖(두께 8mm 미만 등)에서 마지막 행으로 새지 않음
- 반올림 — 올림/내림/사사오입이 엑셀 표기와 일치
- 멀티패스 경계 26.2t · Fill&Pass 38.1t 는 WPS 구간의 중앙값과 정합
- 캘린더 — 400개 조합(교대×실가동×주말×규칙) 퍼징에서 중복 점유·순서 위반·데드락·시간 역행 0건

## 정정 후 기본 시뮬 (58오더 · 1,446본 · 2근)

| | 종전 | 정정 후 |
|---|---:|---:|
| Makespan | 25.0일 | **24.4일** |
| 1위 병목 | JCOE 포장 96.9% | JCOE 포장 **99.3%** |
| 가동률 100% 초과 | 발생 | 없음 |
| 미완주 본수 | 집계 안 됨 | 0본 |

Gap Press 가 줄어든 만큼 상류가 빨라져 **포장 병목이 더 뚜렷해졌습니다.** 포장 산출식
재확인(위 「답이 필요한 것」 1번)의 중요도가 그만큼 올라갔습니다.

---

# 회신 완료 — 2026-08-06

## 확관 제약 · 산출식

> - PPT 자료와 ortools 안의 `specs.py` 가 다르면 **`specs.py` 가 정본**입니다.
> - 12.802m 제품은 **#1·#2호기 모두 투입 가능** — 실제 생산 로그에 Expander #2호기 이력이 있습니다.

| 항목 | 확정 |
|---|---|
| #2호기 길이 상한 | **12.8384m 초과 불가** |
| #1호기 길이 상한 | 14.021m 초과 불가 · 초과 시 #1·#2 동시 가동 |
| 외경 48"↑ / 22"↓ | #2호기 **전용**(hard) |
| R/B 투입 조건 | RB 다이표 외경(24"~48", 46" 제외) & 9 ≤ t ≤ 25.4 & L ≤ 12.8m |
| 확관 횟수 N | **specs.py 식** (#2 짝수 보정 — ①-1 재확인 요청) |
| 셋업 전환시간 | **상위값만 적용** (헤드+다이 동시 교체 → 150분) |
| 공구 매핑 | 외경 ±5mm 안에 없으면 최근접 외경으로 폴백 |
| 계획서 `RunHrs` | **무시** |
| 불량률·보수 소요 | **일단 무시** |

## 설비 구성 — 현장 설비 화면으로 확정

```
JCOE 본류  1차U/T UT109 → 10번 문 → Expander EP103 · EP104 → 면취 FC110 → 수압 HY106
           → 2차U.T UT110 → 관단R/T RT102 → F-X ray RT101(320kV) · RT105(450kV) → JCOE 포장 PK113
R/B 라인   R/B Expander EP102 → R/B 면취 FC112 → [수압 HY106 → 2차U.T UT110] → R/B RT RT104 → 배척 포장 PK112
열처리     옥외 열처리 HT102 → 열처리 포장 PK114   (시뮬레이터 미모델링)
```

- 내면 SAW · **슬러그 제거** · 외면 SAW = **각 4대**, F-X ray **2대**, JCOE 포장 **1대**
- **R/B 는 #1·#2호기 잔여 CAPA 가 없을 때 돌리는 여분 라인** → 기본값 「열처리·배척 제품만」
- **확관 3호기 = R/B 라인** (별도 설비 아님)

## 실 가동시간

| 교대 | 작업 구간 | 휴식 | 실가동 |
|---|---|---|---|
| 1근 | 07:00–11:30, 12:00–15:00 | 점심 11:30–12:00 | 7.5h |
| 2근 | 15:00–18:00, 18:30–23:00 | 저녁 18:00–18:30 | 7.5h |
| 3근 | 23:00–02:45, 03:15–07:00(익일) | 야식 02:45–03:15 | 7.5h |
| 2근연장 | (2근에 추가) 23:30–03:00(익일) | — | +3.5h |

근무 형태가 월마다 달라 **2근 고정**으로 진행합니다.

## 계획서에서 직접 읽게 된 것

| 열 | 값 |
|---|---|
| `용도` | 송유관 / 압력배관용 / 구조용 |
| `재질` | `API-X80L2*CESAG` · `A516-60N*TAZ` … 14종 |
| `물종` | JCOE / **ERW** (JCOE 시트에 ERW 가 섞여 있음) |
| `계획수량` | 수량 열 5종 중 하나 (종전에 수주수량을 잘못 집고 있었음) |
| ~~`영업계획납기일`~~ | **2026-08-14 회신으로 미사용** |

---

# 시뮬레이터가 다루지 않는 범위 — 확인이 아니라 공유

운영 모델(CP-SAT)은 **자재재고·WIP 를 입력으로 받는 스케줄러**이고, 시뮬레이터는
**계획서만 받는 공정 모델**입니다. 아래는 어긋난 게 아니라 **입력이 없어 재현할 수 없는** 항목입니다.

| 정본에 있는 것 | 반영하려면 |
|---|---|
| Ready / Unready 분할, 같은 오더 R·U 동일 호기 강제, Priority 하드 순서 | 자재재고·WIP 파일 |
| CAPA 캡 (`max_total_jobs`) | 캡 값 |
| 긴급(Urgency) 가중 | 계획서 `긴급` 열 (열은 있으나 전부 0) |
| 작업중 오더 설비 핀 (`Fixed_Machine`) | 현재 가동 현황 |
| 각 호기 초기 장착 공구(610×25.4) | 실제 초기 장착 규격 |
| 오더 원자성 (정본은 분할 불가, 시뮬은 본 단위) | — 공정 흐름 관찰 목적상 본 단위가 적절 |

**기본 오더셋에는 `병목 공정 작업장`·`원재료내역` 열이 없습니다.** 기존 산출물에서 추출한
58건이라 열처리·배척 판정이 항상 false 이고, 그래서 기본 화면에서는 R/B 통과 물량이 0본입니다.
실제 계획서 엑셀 또는 **실적 로그**를 올리면 정상 동작합니다.

---

## 참고 — 시뮬레이터에서 직접 확인하는 방법

- **계획서 → 최적화 → 시뮬레이션** — 「계획 실행」 탭에서 한 번에 이어집니다.
- **실적 로그 검증** — 「실적 검증」 탭에 CSV 업로드.
- **정본 ↔ 구버전 제약 비교** — 「확관 최적화」 탭 상단 **확관 제약 기준**.
- **R/B 후처리 경로 전환** — 「확관 최적화」 탭 **R/B 후처리**.
- **확관 N 산출식 / 셋업 산출 / API 5L 판정 / 교대 / 납기 분석** — 「설정」 탭.
- **실적 보정 on/off** — 「실적 검증」 탭 ④ 아래 체크박스.
