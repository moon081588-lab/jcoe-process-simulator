# 세아제강 확인 요청

POSTECH IDEA Lab · JCOE 공정 시뮬레이터
최종 갱신 **2026-08-06**

지금까지 회신해 주신 내용은 전부 반영했습니다. **아직 답이 필요한 것만** 앞에 정리했고,
뒤에 회신 완료 내역과 시뮬레이터가 다루지 않는 범위를 남겨 두었습니다.

---

# 아직 답이 필요한 것

## ① `die_specs_m2` 68행의 출처 ★

“툴 스펙은 표준시간 분석 엑셀의 Expander 페이지에 있다”는 말씀을 받고 엑셀에서 직접 뽑아 대조했습니다.

| | 행수 | `specs.py` 와의 차이 |
|---|---:|---|
| 엑셀 `Expander(1호기)` → mm 환산 | **107** | **0건** (헤드그룹·외경·두께·step 전부 일치) |
| 엑셀 `Expander(2호기)` | **0** | 시트가 **비어 있습니다** (966행 × 62열이지만 값이 있는 셀 0개) |

1호기는 완벽히 일치합니다. 그런데 **2호기 다이표 68행은 엑셀에 근거가 없고 `specs.py` 에만 있습니다.**

**질문** — `die_specs_m2` 는 어디서 가져온 값입니까? 엑셀 시트가 채워진 다른 버전이 있습니까?
2호기 다이 step 이 #2호기 확관 횟수와 소요시간을 그대로 결정하므로 근거 확인이 필요합니다.

## ② 다이표 중복·모호 항목 — 실무 확인 결과 회신 대기

이미 “실제 파일도 그렇다, 실무 확인 예정”으로 답을 주신 건입니다. 결과만 알려 주시면 됩니다.

| 항목 | 현재 처리 |
|---|---|
| `die_specs_m1` OD1219 에 `44t(250mm)` · `44t(700mm)` **중복** | 엑셀 순서상 먼저인 **250mm** 채택 (지시대로) |
| `die_specs_m2` OD914 의 `18.6~38.1t(340mm)` 가 `22.1t(510)` · `28.4t(380)` · `34.8t(380)` 을 구간으로 삼킴 | 원본 로직 그대로. t=22.1 에서 340mm 가 채택되어 확관 39회 (510mm 면 27회) |

## ③ R/B 라인에 API 5L 제품을 보내도 됩니까?

R/B 라인에는 **수압 Test 와 2차 U.T 가 없습니다**(설비 화면 확인).
그런데 API 5L(송유관) 제품은 다이어그램상 **Final U.T + 관단 X-ray** 가 필요합니다.

현재는 R/B 적격이면서 API 5L 인 제품이 R/B 로 가면 그 검사를 건너뜁니다.
현 오더셋에서는 해당 물량이 0건이라 드러나지 않지만, 다른 계획서에서는 발생할 수 있습니다.

**질문** — API 5L 제품은 R/B 라인 투입에서 제외해야 합니까?

## ④ 납기 열은 `영업계획납기일` 이 맞습니까?

계획서에서 `영업계획납기일` 을 납기로 읽고 있습니다. 그런데 값이 계획 시작일보다 과거인 경우가 많습니다.

| 계획서 | 오더 수 | 계획 시작 전 납기 |
|---|---:|---:|
| 2025-12 | 202 | 11건 |
| 2026-01 | 142 | **123건** (지연 총 51,161h, 최대 95일) |

1월 계획서에서는 납기 지연 KPI 가 사실상 의미를 잃습니다.

**질문** — 납기 지연 분석에는 어느 열을 써야 합니까? `실가용일` 이 더 적절합니까?

## ⑤ 실적 데이터

“분석용으로 필요하면 export 해서 드리겠다”고 하신 건입니다.
**시뮬레이션 결과 대조에 3월(또는 12월/1월) 실적이 있으면 좋겠습니다.** 필요한 항목은:

- 오더별 실제 착수·완료 시각 (가능하면 공정별)
- 확관 호기 배정 실적 (#1 / #2 / R/B)
- 설비 정지 이력 (고장·비계획 정지)

MTBF/MTTR 은 현재 미적용(0)이며, 실적이 오면 반영하겠습니다.

---

# 회신 완료 — 2026-08-06

## 확관 제약 · 산출식

> **문세희** — “PPT 자료랑 ortools 안의 `specs.py` 사이에 간극이 있다면 **specs.py 가 맞습니다.**”
> **김명건** — “12.802m → 1·2호기. **실제 로그를 확인해보니 Expander 2호기에서 만든 로그가 있어** ortools 파일 기준으로 보시면 됩니다.”

| 항목 | 확정 | 반영 |
|---|---|---|
| #2호기 길이 상한 | **12.8384m 초과 불가** | 12.802m 제품이 두 호기 모두 투입 가능 |
| #1호기 길이 상한 | 14.021m 초과 불가 | 초과 시 #1·#2 동시 가동 |
| 외경 48"↑ / 22"↓ | #2호기 **전용**(hard) | |
| R/B 투입 조건 | RB 다이표 외경(24"~48") & 9 ≤ t ≤ 25.4 & L ≤ 12.8m | |
| 확관 횟수 N | **specs.py 식** | #1 `round(L/(step−150))` · #2 짝수 보정 |
| #1호기 산출식 | **전달받은 파일 그대로 고정** (ST 수정 중) | 분모 하한 없음 (OD508 t9.5 step170 → N=575) |
| 셋업 전환시간 | **상위값만 적용** | 헤드+다이 동시 교체 → 150분 (합산 아님) |
| 공구 매핑 | **가장 가까운 tool 로 매핑** | 외경 ±5mm 안에 없으면 최근접 외경으로 폴백 |
| 계획서 `RunHrs` | **무시** | 확인 문항에서 제외 |
| 불량률·보수 소요 | **일단 무시** | 변동성 시뮬은 꺼진 기본값 유지 |

## 설비 구성 — 현장 설비 화면으로 확정

```
JCOE 본류  1차U/T UT109 → 10번 문 → Expander EP103 · EP104 → 면취 FC110 → 수압 HY106
           → 2차U.T UT110 → 관단R/T RT102 → F-X ray RT101(320kV) · RT105(450kV) → JCOE 포장 PK113
R/B 라인   R/B Expander EP102 → R/B 면취 FC112 → R/B RT RT104 → 배척 포장 PK112
열처리     옥외 열처리 HT102 → 열처리 포장 PK114   (시뮬레이터 미모델링)
```

- 내면 SAW · **슬러그 제거** · 외면 SAW = **각 4대** (한 라인에 이어져 4열 병렬)
- F-X ray **2대**, 나머지 후처리 설비 각 1대, **JCOE 포장 1대**
- **R/B 는 #1·#2호기 잔여 CAPA 가 없을 때 돌리는 여분 라인** → 기본값을 「열처리·배척 제품만」으로
- **확관 3호기 = R/B 라인** (별도 설비 아님). 별도 설비 토글 제거

## 실 가동시간

| 교대 | 작업 구간 | 휴식 | 실가동 |
|---|---|---|---|
| 1근 | 07:00–11:30, 12:00–15:00 | 점심 11:30–12:00 | 7.5h |
| 2근 | 15:00–18:00, 18:30–23:00 | 저녁 18:00–18:30 | 7.5h |
| 3근 | 23:00–02:45, 03:15–07:00(익일) | 야식 02:45–03:15 | 7.5h |
| 2근연장 | (2근에 추가) 23:30–03:00(익일) | — | +3.5h |

근무 형태가 월마다 달라 **2근 고정**으로 진행합니다.

## 계획서에서 직접 읽게 된 것

실제 조관계획서(JCOE 시트 **76열**)를 열어보고, 대리변수로 때우던 값을 전부 직접 읽도록 고쳤습니다.

| 열 | 값 | 대체한 대리변수 |
|---|---|---|
| `용도` | 송유관 / 압력배관용 / 구조용 | `수량 50본 이상 = API 5L` |
| `재질` | `API-X80L2*CESAG` · `A516-60N*TAZ` … 14종 | `두께 25T 초과 = 고강도` |
| `영업계획납기일` | 전 행 | 납기 미적용 |
| `물종` | JCOE / **ERW** | (JCOE 시트에 ERW 가 섞여 있음) |
| `계획수량` | 수량 열 5종 중 하나 | `수량` 키워드 매칭 (**수주수량을 잘못 집고 있었음**) |

---

# 시뮬레이터가 다루지 않는 범위 — 확인이 아니라 공유

운영 모델(CP-SAT)은 **자재재고·WIP 를 입력으로 받는 스케줄러**이고, 시뮬레이터는 **계획서만 받는 공정 모델**입니다.
아래는 어긋난 게 아니라 **입력이 없어 재현할 수 없는** 항목입니다. 입력을 주시면 반영하겠습니다.

| 정본에 있는 것 | 반영하려면 |
|---|---|
| Ready / Unready 분할, 같은 오더 R·U 동일 호기 강제, Priority 하드 순서 | 자재재고·WIP 파일 |
| CAPA 캡 (`max_total_jobs`) | 캡 값 |
| 긴급(Urgency) 가중 — 정본 목적함수의 2순위 항 | 계획서 `긴급` 열 (열은 있으나 현 계획서에서 전부 0) |
| 작업중 오더 설비 핀 (`Fixed_Machine`) | 현재 가동 현황 |
| 각 호기 초기 장착 공구(610×25.4) → 첫 셋업 최대 270분 | 실제 초기 장착 규격 |
| 오더 원자성 (정본은 분할 불가, 시뮬은 본 단위) | — 공정 흐름 관찰 목적상 본 단위가 적절 |

**기본 오더셋에는 `병목 공정 작업장`·`원재료내역` 열이 없습니다.** 기존 산출물에서 추출한 58건이라
열처리·배척 판정이 항상 false 이고, 그래서 기본 화면에서는 R/B 통과 물량이 0본입니다.
실제 계획서 엑셀을 올리면 두 열을 읽어 정상 동작합니다.

---

## 참고 — 시뮬레이터에서 직접 확인하는 방법

- **계획서 → 최적화 → 시뮬레이션** — 「계획 실행」 탭에서 한 번에 이어집니다.
- **정본 ↔ 구버전 제약 비교** — 「확관 최적화」 탭 상단 **확관 제약 기준**.
- **확관 N 산출식 / 셋업 산출 / API 5L 판정 / 교대** — 「설정」 탭.
- **R/B 증산 시나리오** — 「확관 최적화」 탭 **R/B 라인 투입** → 「적격 제품 전량」.
