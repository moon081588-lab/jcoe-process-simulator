# 세아제강 확인 요청 항목

POSTECH IDEA Lab · JCOE 공정 시뮬레이터
최종 갱신 **2026-08-06** (specs.py 원본 전수 대조 완료)

---

## ✅ 회신 완료 — 2026-08-06 반영

지난번 확인 요청 ①~④ 는 세아제강 회신으로 정리되었습니다. **시뮬레이터 기본값을 회신 기준으로 바꿨습니다.**

> **문세희님** — “제가 업로드한 PPT 자료랑 해당 ortools 안에 있는 `specs.py` 랑 자꾸 데이터 간 간극이 있다고 해서…
> 아 **`specs.py` 가 맞습니다.** PPT 자료 제작 이후에 계속 수정사항이 조금씩 있었어요. PPT 에는 생략된 내용도 있고요.”
>
> **김명건님** — “12.802m → 1호기 2호기, 이거는 **실제 로그 확인해보니 Expander 2호기에서 만든 로그가 있어서
> ortools 파일 기준으로 보시면 될 것 같습니다.**”

| 종전 확인 요청 | 회신 | 반영 |
|---|---|---|
| ① #2호기 길이 상한 12.8m ↔ 12.8384m | **12.8384m** — 12.802m 제품의 #2호기 생산 로그 실재 | `EXP_RULESET.ortools` 를 기본값으로 |
| ② 확관 N 산출식 (엑셀 홀수 ↔ specs.py) | **specs.py** | `EXP_NMODE` 기본값 `ortools` |
| ③ RB 투입 조건 (OD 24"↓ ↔ 다이표 24~48") | **specs.py (다이표 · 9≤t≤25.4)** | `rbCapable` 기본 `ortools` |
| ④ 외경 48"↑/22"↓ 우선 ↔ 전용 | **전용(hard)** | `m2Exclusive: true` 기본 |

**결과가 이렇게 바뀝니다** (EAT · 2교대 7.5H · 공구 계층 셋업)

| | 종전 기본값 (PPT 기준) | **새 기본값 (specs.py)** |
|---|---:|---:|
| Makespan | 28.4일 | **32.4일** |
| 확관 #1 / #2 처리 본수 | 1,354 / 417 | **804 / 899** |
| 확관 호기 부하 편차 | 141.0h | **3.6h** |
| 1위 병목 | 포장 및 출하 85% | 포장 및 출하 75% |
| 최적화(OPT) 적용 시 | 26.3일 · 전환 56.5h | **30.4일 · 전환 42.0h** |

`ortools_final_v2_ep12` 원본을 받아 전량 대조했습니다 — **다이 스펙 212행 전부 일치,
#1·#2호기 셋업 4,050쌍 전부 일치.** 옮겨 적는 과정의 오류 3건은 찾아서 고쳤습니다
(두께 ±0.1mm isclose 규칙 누락 · `die_id` 입력외경 기준 · #1호기 N 분모 하한 제거).

> 종전에 보고드렸던 **“확관 호기 부하 편차는 배분 규칙이 아니라 제품 길이 구성 때문”** 이라는 진단은
> PPT 기준에서만 성립하는 결론이었습니다. specs.py 기준에서는 **편중 자체가 없고**, 배분 규칙·최적화가
> 손댈 수 있는 물량이 111본 → 941본으로 8배 늘어납니다.

두 기준은 화면에서 계속 전환해 볼 수 있게 남겨 두었습니다 —
「확관 최적화」 탭 **확관 제약 기준**, 「설정」 탭 **확관 N 산출식**.

---

## ✅ 원본 데이터로 해결 — 2026-08-06 (2차)

`ortools_final_v2_ep12\data\` 안의 **실제 조관계획서**(`1. 2025년 12월 포항공장 조관계획서.xlsx`, JCOE 시트 **76열**)와
자재 조회 파일을 열어보고, 그동안 "계획서에 열이 없다"고 가정했던 항목들이 **사실은 다 있다**는 것을 확인했습니다.
로더가 읽도록 반영했습니다.

| 종전 확인 요청 | 실제 계획서 | 반영 |
|---|---|---|
| API 5L 여부를 알 수 없어 `수량 50본 이상`을 대리변수로 | **`용도`** 열 — 송유관 28 / 압력배관용 168 / 구조용 6. **`재질`** 열 — `API-X52L2` · `API-X65L2I` · `API-X70L2` · `API-X80L2*CESAG` … 두 값이 정확히 일치 | `용도=송유관` 또는 `재질`이 `API-` 로 시작하면 API 5L. **대리변수 제거** |
| 재질 열이 없어 `두께 25T 초과 = 고강도` 대리변수 | **`재질`** 14종 — `A516-60N*TAZ`(161) · `API-X70L2` · `A572-50` · `SS400` … | `X70` 이상 = 고강도. 재질 열이 없을 때만 두께 대리변수로 폴백 |
| 납기가 없어 지연 분석 불가 | **`영업계획납기일`** 열 | 납기 지연 집계 동작. 12월 계획서 202건 중 **11건 지연** |
| (인지 못했던 문제) | **`물종`** 열 — JCOE 시트에 **ERW 2건이 섞여** 있음 | JCOE 아닌 행은 제외 |
| (인지 못했던 문제) | 수량 열이 **5종** — 수주수량 / 오더수량 / **계획수량** / 조관수량 / 포장수량 | 운영 모델이 쓰는 **`계획수량`** 을 최우선 인식. 종전에는 더 왼쪽의 `수주수량` 이 잡혀 **1,522본 vs 1,003본, 52% 과대**였음 |

실제 12월 계획서를 넣으면 **202오더 / 1,003본**으로 읽히고, pandas 로 직접 센 값과 정확히 일치합니다.

> 자재 조회 파일(`251202 16시 조회.XLSX`)에도 `재질` · `제품용도` · **`열처리`** · `PIPE_SPEC` 열이 있습니다.
> 열처리 여부를 자재 단계에서 직접 알 수 있으므로, `병목 공정 작업장 = HT102` 판정과 교차 확인이 가능합니다.

---

# 남은 확인 요청

아래는 아직 근거가 없어 **추정으로 채운 항목**입니다. 영향이 큰 순서입니다.

## ① 병렬 설비 대수 ★최우선

자료에 대수 표기가 없어 가정한 값입니다. **틀리면 병목 순위가 바뀝니다.**

| 설비 | 현재 가정 | 확인 필요 |
|---|---:|---|
| 내면 SAW / 외면 SAW | 각 4라인 | 다이어그램 “4개 라인 병렬 운영” 근거 — 맞는지? |
| **슬러그 제거** | **1대** | 4라인 병렬 안에 포함인지 아니면 단독 1대인지? (가동률 61%, 2위 병목) |
| **포장 및 출하** | **1라인** | 몇 라인인지? (가동률 75%, **1위 병목**) |
| 관단 X-ray | 1대 | |
| F-X ray | 2대 | 다이어그램 “Max 3대” — 상시 몇 대 가동인지? |
| 면취기 / 수압 / CP | 각 1대 | |

민감도 — **슬러그 4라인 + 포장 2라인**이면 32.4일 → 31.3일이 되고
**1위 병목이 포장에서 Expander(53%)로 넘어갑니다.**
즉 “확관이 병목인가”라는 명제의 성립 여부가 이 두 설비의 대수에 달려 있습니다.

## ② 계획서의 `RunHrs` 가 우리 산출값의 2배입니다 ★신규·중요

실제 계획서에 세아제강이 직접 산출한 **`RunHrs`** 와 **`병목 공정 RunHrs`** 열이 있습니다.
병목 공정이 확관(`EP101`)인 오더 20건만 뽑아 우리 엔진의 확관 소요시간과 대조했습니다.

| 오더 | 규격 | 본수 | 세아 병목 RunHrs | 우리 확관 | 비 |
|---|---|---:|---:|---:|---:|
| 1088695 | OD914×t17.3×18m | 55 | 28.75h | 9.85h (동시가동) | 0.34 |
| 343573 | OD914×t25×12.802m | 24 | 4.92h | 3.10h | 0.63 |
| 352766 | OD1219×t25×12.802m | 15 | 3.08h | 1.56h | 0.51 |
| 354589 | OD1219×t9.3×12.802m | 3 | 0.61h | 0.28h | 0.45 |
| **합계 20건** | | | **59.3h** | **27.5h** | **0.46** |

두 가지가 걸립니다.

1. **우리 산출이 절반 수준**입니다. `specs.py` 의 `calculate_time_m2` 를 그대로 구현했는데도 그렇습니다.
2. **`병목 공정 RunHrs` 는 길이에만 반응합니다** — 본당 6.0m 0.100h / 12.192m 0.155h / 12.802m 0.205h / 18m 0.523h 로,
   같은 길이면 **외경·두께가 달라도 값이 같습니다**. `specs.py` 는 다이 step(외경·두께)으로 확관 횟수를 정하는데,
   계획서 쪽은 그렇지 않습니다.

**질문**
- `RunHrs` / `병목 공정 RunHrs` 는 각각 **무엇을 포함한 시간**인지? (셋업 포함? 여유율·수율 반영? 라인 점유 기준?)
- 계획 수립에는 어느 값을 쓰는지 — 계획서의 `RunHrs` 인지, `specs.py` 산출식인지?
- 둘이 2배 차이 나는 이유를 알 수 있을까요? 시뮬레이터를 어느 쪽에 맞춰야 하는지 판단이 필요합니다.

---

## ③ RB 라인 통과분이 면취·수압·검사를 건너뜁니다 ★신규

현재 라우팅은 **RB 라인 → 포장 및 출하 직행**입니다. 확관 #1·#2호기로 간 파이프는
Calibration Press → 면취기 → 수압 → (Final U.T·관단 X-ray) → F-X ray 를 거치는데,
RB 로 간 **93본**은 이 공정을 전부 건너뜁니다.

그 결과 수압(50.0%) · 면취기(49.8%) · F-X ray(42.4%) 가동률이 각각 6.9%p 정도 과소평가됩니다.

**질문** — RB 라인에서 확관한 제품도 면취·수압·검사를 거치는지?
거친다면 라우팅을 고치겠습니다. (운영 모델 `optimizer_grouped.py` 는 확관 공정만 다루어 후공정 정보가 없습니다)

---

## ④ 확관 다이 스펙표의 중복·모호 항목

원본 `specs.py` 를 그대로 재현했음을 확인한 뒤에도 아래는 **표 자체를 봐야** 판단이 됩니다.
지금은 원본 로직을 그대로 따르고 있으니, 표를 고쳐야 하는 것인지 확인 부탁드립니다.

- **`die_specs_m1` OD 1219 에 `44t(250mm)` 와 `44t(700mm)` 가 중복**되어 있습니다.
  두께 조건이 같아 판정이 불가능하고, 원본 로직상 **먼저 나온 250mm** 가 채택됩니다.
  12.802m 기준 250mm → N=128 / 1,767초, 700mm → N=23 / 507초로 **3.5배 차이**입니다.
- **`die_specs_m2` OD 914 의 `18.6~38.1t(340mm)`** 가 같은 외경의 `22.1t(510)` · `28.4t(380)` ·
  `34.8t(380)` 을 구간으로 전부 삼킵니다. t=22.1 에서 구간 표기가 먼저 나오므로 340mm 가 채택되어
  N 이 39회가 되는데, `22.1t(510mm)` 를 쓰면 27회입니다.
  (t=18.8 은 `18.9t(580mm)` 와 ±0.1mm 안이라 580mm 가 채택됩니다 — 이건 원본과 일치 확인했습니다.)
- **`die_specs_m2` 에 OD 1473 · 1575 가 없습니다.** 해당 외경 제품이 들어오면 공구 판정이 불가합니다.
- **다이표에 없는 규격 사이의 셋업이 0초입니다.** `get_tool_info` 가 `(None, None, ("UNKNOWN","UNKNOWN_DIE"))`
  를 반환해, 서로 완전히 다른 규격끼리도 「교체 없음」이 됩니다.
  시뮬레이터는 이 경우 **최소 다이 교체 90분**을 보수적으로 부과하고 「공구 미상」으로 표시합니다.
  **어느 쪽이 맞는지?** (원본대로 0 으로 되돌릴 수도 있습니다)
- **RB 적격 판정의 외경 기준이 파일 안에서 엇갈립니다.** `job_creator.py` 는
  `if d not in valid_rb_diameters` 로 **정확히 일치**를 요구하는데, 같은 흐름에서 쓰는
  `specs.get_tool_info` 는 **±5mm 근사**를 씁니다. 계획서에 OD 660.4 · 711.2 처럼 소수점이 붙어 오면
  RB 적격 여부가 갈립니다. 시뮬레이터는 ±5mm 로 통일해 두었습니다. **의도된 차이인지?**
- **#1호기 N 산출식의 분모가 작은 step 에서 급격히 커집니다.** `round(L / (step−150))` 이라
  OD508 t9.5(step 170) 에서 분모가 20 이 되어 11.5m 제품에 **N=575회 · 7,127초**가 나옵니다.
  원본 그대로 반영했지만, 스트로크당 20mm 전진이 물리적으로 맞는 값인지 확인 부탁드립니다.
  (표상 step 110~200mm 구간이 여기 해당하며, M1 전체 step 31종 중 11종입니다)

## ⑤ 확관 셋업(전환) 시간 — 실측치 확인

`get_setup_time_val` 을 그대로 이식했습니다.

| 바뀌는 공구 | 셋업 |
|---|---:|
| Drawbar | 270분 |
| Head Group | 150분 |
| Die | 90분 |
| 동일 | 0 |

- 이 값이 현재 현장 실측 기준으로 유효한지?
- 동시에 두 가지가 바뀔 때(예: 헤드 + 다이) 합산이 아니라 **상위 값만** 적용하는 게 맞늕지?

## ⑥ 확관 3호기 = RB 라인 확인

제약표의 “확관 3호기”가 RB 라인과 같은 설비로 이해했습니다.
근거는 “확관 3호기: 열처리 공정 제품 & 배척 제품 우선 투입”이
`Force_RB`(병목공정 `HT102` = 열처리 / 원재료·제품 길이비 ≥ 1.8 = 배척)와 정확히 대응한다는 점입니다.
**확관 3호기 = RB 라인이 맞는지?** (아니라면 별도 설비 토글을 기본 ON 으로 바꾸겠습니다)

## ⑦ 그 외 미확인

| 항목 | 현재 값 | 비고 |
|---|---|---|
| 불량률 (F-X ray) | 2% | 변동성 시뮬레이션 기본값 — 실적 필요 |
| 불량 유형 비율 | 용접 65% / 확관 35% | 재확관율 실적 필요 |
| Repair / 보수용접 / 재확관 소요 | 30분 / 60분 / 45분 | 추정치 |
| 설비 MTBF / MTTR | 미적용(0) | 실적 있으면 반영 |
| ~~납기(요청일)~~ | **해결** — `영업계획납기일` 열 | |
| 실 가동시간 | 7.5H / Shift | 엑셀 기준 — 현재도 유효합니까? |
| 3월 실적 | 없음 | 시뮬레이션 결과 대조용 |

---

## ⑧ 운영 모델과 시뮬레이터의 범위 차이 — 확인이라기보다 공유

운영 모델(CP-SAT)은 **자재재고·WIP 를 입력으로 받는 스케줄러**이고, 시뮬레이터는 **계획서만 받는 공정 모델**입니다.
아래는 어긋난 게 아니라 입력이 없어 재현할 수 없는 항목입니다. 필요하시면 입력을 받아 반영하겠습니다.

| 정본에 있는 것 | 시뮬레이터 | 반영하려면 |
|---|---|---|
| Ready / Unready 분할, 같은 오더 R·U 동일 호기 강제, Priority 하드 순서 | 전량 Ready 가정 | 자재재고·WIP 파일 |
| CAPA 캡 (`max_total_jobs`) | 계획서 전량 투입 | 캡 값 |
| 긴급(Urgency) 가중 — 정본 목적함수의 2순위 항 | 미반영 | 계획서 `긴급` 열 |
| 작업중 오더 설비 핀 (`Fixed_Machine`) | 미반영 | 현재 가동 현황 |
| 각 호기 초기 장착 공구 610×25.4 → 첫 셋업 최대 270분 | 첫 셋업 0 | 실제 초기 장착 규격 |
| 오더 1건 = 분할 불가 구간 | 본 단위 (39개 중 21개가 두 호기에 걸침) | — 공정 흐름 관찰 목적상 본 단위가 적절 |
| RB 를 solver 가 M1·M2 와 함께 고름 | 「적격 전량 RB」/「Force_RB 만」/「미사용」 3모드 | ④ 외부 스케줄 가져오기로 대체 가능 |

또한 **`Force_RB` 판정 근거 데이터가 기본 오더셋에 없습니다** — `병목 공정 작업장`·`원재료내역` 열이
기존 산출물에서 추출한 58건에는 들어 있지 않아, 열처리·배척 판정이 항상 false 입니다.
계획서 엑셀을 올리면 두 열을 읽어 정상 동작합니다.

---

## 참고 — 시뮬레이터에서 직접 비교하는 방법

- **정본 ↔ 구버전 제약 비교** — 「확관 최적화」 탭 상단 **확관 제약 기준**.
  분류 물량·가동률·소요일이 즉시 다시 계산되고, 두 기준 비교표가 그 아래에 나옵니다.
- **확관 N 산출식 / 셋업 산출** — 「설정」 탭.
- **계획서 → 최적화 → 시뮬레이션** — 「계획 실행」 탭에서 한 번에 이어집니다.
  다른 조관계획서 엑셀을 올리면 그 계획서로 전부 다시 계산됩니다.
